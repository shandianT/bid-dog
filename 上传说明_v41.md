# v0.16.0 上传说明(第 41 包,含此前全部内容)

**v0.15.0 两件事,都是你提的:**
①**实时工作台词**——对话区常驻控制台,agent 每一步在做什么逐行流式显示(读哪个文件/跑什么脚本/写出什么),
每步有阶段分隔线,跑完自动折叠成回放;
②**预计等待时间回来了**——引擎记录每步真实耗时(滚动平均,越用越准),前端按"当前步剩余+后续各步均值"估算,
第 1 步就有数,不被单个超长步带偏;没历史时用参考值并标"约"。
另:官网首页截图墙换成**真实 Word 成品页**(封面/图文正文/偏离表/章节图文),删掉应用界面截图。
v0.15.1:在线体验版的台词补齐真实内容(原来只有分隔线),台词标题去掉"流式"字样。
v0.16.0 接入体验重做(用户只做两件事):
① 快速接入 = 粘一串 Key + 选模式 → 生成引擎/对话/**识图**全部自动配好
   (修真 bug:识图模型此前因字段名写错一直没配上);
② 两种模式:标准(senseaudio-s2,写正式标书)/ 极速(deepseek-v4-flash,试跑更快更省);
③ 生成模型可选:真下拉 +「取模型」实拉你套餐里全部模型,摘要行显示当前用谁写;
④ 引擎选项改「自动(默认)」:有 Key 产真实标书,没 Key 自动回落演示并说明原因;
⑤ 高级只留必要项。

## 怎么上传
https://github.com/shandianT/bid-dog → Add file → Upload files → 本文件夹全部内容
(含 .github 隐藏文件夹,macOS 按 Cmd+Shift+.)→ 提交 main → Actions 构建 15~25 分钟 → Releases desktop-v0.16.0。
