# 中标狗 v0.20.4 优化说明(对照实测反馈逐条回应)

> 基线:v0.20.3。本版针对 2026-08 一线实测反馈 PDF 的 9 个问题逐条根因定位并修复,
> 同时主动扫描并修复了一批同源的潜在问题。Windows / macOS 双平台适用。
> 验证:Python 回归 380 项通过、前端逻辑 47 项通过、Chromium 交互 13 项通过。

## 反馈问题逐条回应

### 1/2. 素材库与参考资料格式支持有限(仅 docx/md)

**根因**:pypdf 早已是打包依赖(招标主件解析一直在用),但素材入库端点没写 PDF 分支;
.doc 无纯 Python 解析器被一刀切拒收;前端文件选择器同步收紧。

**修复**:
- 素材库入库支持 **PDF**(文字层提取 + 中文编号行「第X章/一、/1.1」自动升为标题拆章;
  扫描件给出「请先转 Word」的明确指引而非笼统报错)。
- 支持 **.doc**:魔数三分流——改后缀的假 doc(实为 docx)直接解析;RTF 本地提取(WPS 常见);
  真 OLE 老格式自动调用系统转换器(macOS textutil / Windows Word COM / LibreOffice),
  都不可用时明确提示「另存为 .docx」。
- 支持 **HTML** 与**图片直接入库**(png/jpg/webp/gif/bmp 落 图片/ 并登记索引)。
- 参考资料同样放开格式并支持**多选上传**;流水线读素材同步支持 .doc/.html
  (旧版这些格式「上传成功」但内容一个字进不了模型,静默黑洞)。

### 3. 图片自动打标与材料落库

- 入库出现新图片且已配视觉模型时**自动开始打标**,不用再手点按钮(加锁防止并发双开)。
- 建任务导入的材料**自动回流素材库**「任务导入/」分类(按 md5 与库内去重,不堆副本),
  下一单直接复用;向导里新增默认勾选的「同时存入素材库」开关,可关。
- 修复隐患:无素材任务上传参考资料会被写进**全局素材库**,再被合并进之后每个新任务
  (A 项目的旧标书串进 B/C/D 项目)。现在参考资料恒定归属本任务。

### 4. 图片打标失败(合同截图类 100% 失败)

**根因**:视觉调用 `max_tokens=500`,合同截图 OCR 文本长,JSON 输出在 500 token 处被硬切,
解析停在字符串中间(报错列号 520/539 正是截断点);解析是裸 `json.loads`,无容错无重试。

**修复**:max_tokens 提到 1000;宽容 JSON 解析(转义裸控制字符→补关截断字符串→
逐步砍尾部残缺键值对);失败自动带更严格指令重试一次;仍失败**降级登记**
(类别=其他、图注=文件名),图不再掉出索引,界面提供「重试失败图片」一键入口;
错误信息人话化,原始异常收进技术详情折叠区。

### 5. 任务导航栏状态不准(「模型连接」「实际运行时间」)

- 「模型连接」检查项从「启动时的静态判断、之后永不更新」改为**随真实连接事件更新**:
  探活成功/首包到达置绿,断连置红(带原因),自动恢复后置回。
- 「实时连接」徽标其实只反映界面↔本地服务的进度同步,与模型无关——文案改为
  「进度同步中/进度同步中断」,不再被误读成模型状态。
- 「实际 0秒 · 通常 1分钟」不再出现(耗时为 0 不硬凑);「停顿 15 小时 42 分 未计」
  这类内部口径改为「共用 X(中途等待 Y)」。
- 任务事件流改为增量读取,长任务多标签页下状态不再滞后。

### 6. 任务中断误报

三个独立误报源全部修复:
- **SSE 半开假死**:事件流 socket 超时 600 秒比停滞判定阈值(480 秒)还长,连接半开时
  先触发误报中断。收紧到 180 秒,并新增**消息体活动签名**第三路心跳——模型在长章节
  生成期间即使不写文件、事件流静默,只要还在吐字就绝不判卡死(旧版会亲手打断正常运行的一轮)。
- **前端历史事件回放**:每次打开任务都会重放全部历史事件,已被自动恢复消化掉的旧错误
  会再次弹「任务没有按预期继续」横幅——运行中/已完成、文件完好的任务也被报中断。
  现在只有新鲜错误且任务确实未在跑时才弹;有新进展自动清横幅。
- **引擎重启孤儿任务**:崩溃/睡眠/强关后重启,任务显示「意外中断」且重试被拒。
  现在启动时自动对账:归位到检查点、给出「从已保存内容继续」。
- 节点生成期间每 30 秒心跳,超过 3 分钟的正常长生成不再被标「响应偏慢」。
- 终态诚实归因:连接中断/上游限流/输出未通过分别写明,不再一律「连接中断」。

### 7. 创建任务导入的材料未显示

- 建任务时记录「本单亲手导入」清单(与后续全局库合并件区分开);任务详情右栏
  分「本单导入素材 · N 个」「参考资料 · N 个」两组展示。

### 8. Debug 功能不实用 + 重试导出报错

- **Word 导出失败根因**(`Package not found at ..._MEI...default.docx`):PyInstaller onefile
  把 python-docx 模板解压到系统临时目录,Windows 磁盘清理在引擎跨夜运行后把它删了。
  三层修复:桌面壳把引擎 TMP/TEMP 指到数据目录受控子目录(系统清理碰不到);
  引擎启动备份模板并经环境变量下发;技能包导出脚本模板缺失时自动用备份。
- 一键诊断新增第 4 项「**本单执行**」:任务失败时不再三项全绿误导;置顶人话结论
  (停在哪个节点、直接原因、建议动作);「技术详情」输出可读文本,原始 JSON 只进诊断包。
- 「查看运行日志」由事件流+诊断流水合成带时间戳的可读时间线,不再是一句
  「当前运行方式没有文本日志」。
- 「立即补出 Word」失败按真实原因返回(模板缺失给「重启应用即可恢复」指引),
  不再误报「没有正文稿请重跑」(重跑既烧额度又治不了模板缺失)。

### 9. 生成频繁中断且无法续跑(6 次仅 1 次成功的核心)

- **流式请求改造**(最大元凶):旧版每个模型节点是一次非流式长请求(固定 240 秒超时),
  要等整段生成完才有首字节;国内中转网关普遍 60-100 秒空闲即掐连接,章节动辄 2-4 分钟,
  必然被掐。改为流式:模型只要在吐字连接就有流量,连续 90 秒无新内容才判死;
  断流保留半截内容按截断续写(不浪费已生成的);网关不支持流式自动降级。
- 节点重试预算 3→5 次,连接/限流类错误按 8s→24s→72s→216s 分钟级退避
  (旧版三连击共约 40 秒,等不到一次真实的网络恢复);退避中点「停止」秒级生效。
- 自动恢复连败预算随真实进展清零、整单上限 12 次(旧版整单只给 2 次,
  几小时的任务断两次流就永久停)。
- **「重试没有启动」三重修复**:前端 `loadJobs` 未定义导致重试成功也报
  「重试没有启动」(ReferenceError 被吞)——已定义;后端 worker 僵尸占位自动回收;
  同节点重复点击返回「已在进行中」而非报错。
- 同网关**换 Key 不再作废检查点**(旧版用户失败后换 Key 自救,反而导致已写章节全部重打)。
- Windows 上用户用 Word/WPS 打开中间产物导致的文件锁改为可重试,不再把整单打死。
- 素材超上限(1000 文件/100MB)改为截断+诊断,不再抛异常终止
  (旧版素材库养得越大、任务越必然失败)。

### 总结反馈:「UI 不够完善,一线售前不知道怎么用」

- 流程黑话人话化:体检素材→检查资料、读懂组成→分析招标文件、评分废标→找评分点与
  废标条款、逐条应答→逐条响应要求、汇总成册→合成全文…;LIVE ORCHESTRATION→生成进行中;
  出件前→提交前。
- 按钮语义澄清:「继续做」→「从断点继续」、「继续修改」→「修改结果」(悬停有说明)。
- 中断态展示「已保住 N 个过程文件」+「打开任务文件夹」出路,不再只说「去看诊断」。
- 首次引导:速度模式文案与实际默认一致;补充「Key 是什么、找谁要」。
- 「先看一遍演示流程」入口在引擎在线时也可用(旧版桌面端 100% 点不动)。
- 补齐从未定义的 --soft/--muted CSS 变量(快速接入卡、工作日志框底色失效的视觉退化)。

## 主动修复的其他潜在问题

- /v1/assets 上传接口路径穿越(category/filename 未净化可写任意本地路径)。
- doc_quality 解析 WPS 转存 docx 时 p.style 为 None 崩溃(参考资料静默失效)。
- oc_watch 事件订阅重连时重取 server 地址与口令(旧版 server 重启后台词永久静音);
  连续失败记诊断并提示用户,不再无声死循环。
- 瞬时网络错误判定补 ConnectionRefused/DNS 失败(路由器/VPN 重连窗口)。
- CI 构建依赖锁定版本范围(requirements-build.txt);PyInstaller spec 关闭 UPX
  (Windows 杀软误报重灾区)并与 CI 参数对齐。

## 构建与验证

- 构建方式不变:见 `BUILD.md`;GitHub Actions workflow_dispatch 触发双平台安装包构建。
- 回归验证(本包内可复现):
  - `tests/run_all.sh`:Python 380 项 + 前端逻辑 47 项 + Chromium 交互 13 项,全部通过
    (1 项 Windows 专属检查按平台跳过)。
- 建议发布前在 Windows 真机补一轮长任务实测(跨夜挂机 + 中途断网恢复 + Word/WPS
  开着中间文件时重试导出)以覆盖沙箱无法模拟的场景。
