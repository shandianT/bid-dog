# 更新日志

本文件记录中标狗桌面端的重要产品变化。

## v0.20.5 体验回归批次(交互与观感)

真机走查一轮界面后修的一批「说的和做的不一样」:

- **「从断点继续」按钮永远不出现**:`/v1/jobs` 对每一单都下发 `runtime.capabilities={pause:{…}}`,
  前端拿它整体遮蔽了 `can` 列表,`can` 里的 `resume` 再也读不到。任务写着「可从检查点继续」,
  顶栏却一个继续的入口都没有——用户被困在半成品任务上。改为两个来源合并,
  且 `can` 只对它真正枚举的动作有发言权(否则归档会被连坐关掉)。
- **重开应用后进度条清零**:右栏只信任本次会话的 SSE 步数,重开后回落成一排灰点加「启动中」,
  而检查点明明在第 8 步。改为与已落盘的检查点取大值,与引擎口径一致。
- **停下的任务还在转菊花、还在倒计时**:改为「⏸ 已停止 · 停在某阶段」并停表。
- **回放事件把「已停止」改写成「运行中」**:重连时 SSE 会整段回放历史,每条 progress 都在
  覆盖服务端的权威状态。改为只有新鲜事件才有资格改状态,历史只补进度数字。
- **「没出 Word,未完成」红牌误报**:按一下就能接着跑的任务被判成交付物丢失。
  改为只有确实没路可走(拿不到 resume)时才判红。
- **「实际 8 小时 · 通常 1 分钟」**:环境准备阶段把用户建完任务去吃饭、隔天才点开始的空档
  算成了机器耗时(实测能到 29 天)。引擎不再拿墙钟兜底,前端对不可信数值退回「通常 X」。
- **文案与实际状态打架**:流程台不再写死「生成进行中」;排队节点不再显示「尚未提供节点详情」;
  「出Word质检」在两个面板叫两个名字,统一成「生成 Word 并检查」;
  评分点覆盖不再出现左边「落位:技术方案」右边「未定位到章节」的自相矛盾(原因改由后端给)。
- **观感与导航**:任务名不再被按钮挤成「某某医院智…」(按钮先换行);折叠分组里钉住当前任务,
  不会出现中栏开着某单、左栏一条都不亮;大纲空状态给出「现在该做什么」和一颗真按钮。
- **自动更新交互**:点下去后入口变进度态并锁定,不再只有一闪而过的 toast 和可重复触发的并发下载;
  官网下载链接补到 v0.20.5。

回归:Python 400 通过(新增环境阶段耗时、覆盖原因 2 项);前端逻辑 51/51(新增能力合并、
进度回落、交付死路、耗时可信度 4 组);Chromium 交互 23/23(新增 check_recovery_ui.spec.js 5 项)。

## [未发布]

## [0.22.0] - 2026-09-02

### 响应规划接回模型:评分点覆盖终于是真的

默认流水线里,「响应规划」这一步自 0.20.4 起就完全不调模型——那次是为了止血
(模型返回被截断把整单卡死在章节撰写之前),但止血成了永久状态:评分点矩阵只是
含「分」字的行,「响应位置」是一句常量,「缺口」全是〔需补充〕。后果是一整串功能
一起失效:顶栏「评分点覆盖」永远 0/N;覆盖面板里的「补写应答」和按章批量一条都
派不出去;单章重写「自动带入本章漏项」永远为空;章节提示词里的「评分点」是含'分'
字的句子而不是本章要拿的分;解析确认卡「共 N 个评分点」数的是行数。

现在分两步:

- **本地索引先落盘**(零 token、秒级,永远兜底);
- **模型只做逐项核对**:从评分办法/评标办法/资格审查表逐项提取评分点,把每一项
  落到真实章节标题上(只认目录里的标题,「第 2 章」这类写法也认),分值统一成「N 分」,
  无缺口写「无」;分值合计与评分办法总分对不上会写进矩阵备注并记诊断。
  核对失败/超时/格式不对,有限重试后沿用本地索引,节点照样完成——不会再拖垮整单。
- 落到某章的评分点直接进该章的撰写提示词——这才是「本章要拿的分」。
- `response_plan.json` 与三份规划文件都记录规划来源;`/v1/jobs/{id}/coverage` 透出
  `plan_source`,界面据此说话:模型核对过 → 真实覆盖率;只有本地索引 → 「评分点 N 项 · 待核对」,
  覆盖面板说明原因、不画覆盖条、不给补写按钮,确认卡改说「识别到 N 处评分相关条款(候选)」。
  0/N 不再出现。解析版超预算时优先整段喂评分/评标/资格/否决章节。
- `BIDDOG_PLAN_MODEL=0` 可关掉模型核对(回到纯本地);`BIDDOG_PLAN_CONTEXT_CHARS`、
  `BIDDOG_PLAN_MAX_TOKENS` 可调。

新增 `tests/test_response_plan_model.py`(7 条)与 `tests/next/check_plan_source.spec.js`(2 条);
原「规划不调模型」那条测试改为钉住「本地索引先写好、模型失败也完成」。

### 修改指令路由到单章 · 章节持续 4 路在飞 · 生成参数可配 · 章节撰写契约

- **「继续修改」先判范围。** 以前一律建子任务从头跑全部节点——写「第三章售后响应时间
  改成 2 小时」要等所有章节重写完。现在停顿 400ms 就问引擎 `POST /v1/jobs/{id}/revisions/plan`:
  指令指到具体章节(第 N 章 / 章节标题 / 技术或商务偏离表)且不涉及整册与版式 → 走单章重写
  通道(旧稿进历史版本,汇总与 Word 自动跟着更新);涉及多章、整册、版式或没指到章节 → 整册
  新版本。范围先亮出来,用户可改、可换章。
- **章节持续 4 路在飞。** 以前 `batch = ready[:2]` 两章一批互相等,10 章 20 分钟串行;现在
  一章写完立刻补位(`BIDDOG_CHAPTER_PARALLEL`,默认 4,上限 8),某一章撞到 429 限流后这一单
  余下章节自动降到 2 路。
- **生成参数可配。** `PUT /v1/agent` 接受 `generation_params`(temperature / frequency_penalty /
  presence_penalty,越界钳到范围内),`GET /v1/agent` 回显;任务跑着时不许改(A/B 的可比性靠这个)。
  默认值不动——没有证据不改默认。`tools/model_ab.py` 支持参数变体
  `"deepseek-v4-flash?temperature=0.5&frequency_penalty=0.4"`,同一模型两组参数在结果表里分得开。
- **章节撰写契约独立成文(`server/prompts.py`)。** 论述章的 system 不再是把多 agent 版 SKILL.md
  用关键词正则压出来的零散句子,而是一份不到 1500 字的结构化契约:事实边界、小标题按本章现拟
  并**传入其他章已用过的小标题清单**(SKILL 第 4 步的纪律,流水线以前没做)、表格装真内容、
  配图只证明响应、资质整块留位。篇幅线统一成一个常量 `CHAPTER_TARGET_CHARS = 3500`
  (以前提示词 2500、OpenCode 路径 800、质检黄线 3500,三处三个数)。

新增 `tests/test_p1_pipeline.py`(19 条)与 `tests/next/check_revision_route.spec.js`(2 条)。

### 素材按章检索 · 《评标索引》与《投标人补料清单》 · 模型复核闭环

- **素材按章检索喂料。** 以前每个章节节点把整个 `素材/` 拷进尝试目录,再按文件名顺序读到
  14 万字预算——素材库越大,排在后面的文件越被截光,而且每一章都在重复读同一堆无关的章节模板
  (token 成本大头)。现在根目录的规范素材(能力表/应答要点/公司介绍/资质案例/图片索引)永远带上;
  子目录(章节模板/、任务导入/)按本章标题、素材槽位、评分点的关键词命中排序,装到预算为止
  (`BIDDOG_CHAPTER_MATERIAL_CHARS`,默认 6 万字节);图片等二进制不进节点目录。喂给模型的顺序
  改成「声明输入 → 规范素材 → 相关模板」,招标解析版不会再被素材挤掉;跳过了多少份记进诊断。
- **《评标索引》与《投标人补料清单》。** SKILL 第 4.5/5 步要求、流水线以前没有的两份整册级产物。
  评标索引(评分点 → 分值 → 证据 → 对应章节,合计分)放整册最前,只在规划经模型核对过时才出——
  本地索引印出来是一张全〔需补充〕的表;补料清单放整册最后:正文里所有〔需补充〕〔此处粘贴〕
  〔参数待核实〕〔配图建议〕按章去重汇总,加废标风险清单的提交前动作,加固定的人工确认节点。
  两份都另存单独文件,右栏能直接点开(索引归「分析依据」,清单归「检查报告」),不会被当成正文。
- **模型复核闭环。** 标准模式多跑一步模型复核,以前产出只是右栏里一个《模型复核报告.md》,
  多花的那份钱没有闭环。现在复核提示词要求固定表头 `| 章节 | 问题 | 级别 | 修订建议 |`、级别只能是
  必办/建议;引擎解析这张表并入出件前检查:必办 → 红,每条直接挂「重做这一章」(问题与修订建议
  作为重写要求带过去);建议 → 黄。
- **`app.css` 只服务原型。** 主界面误载入了整份原型样式,与 `live.css` 的规则互相覆盖;
  现在只在 Showcase 里加载,主界面需要的滚动条样式补进 `live.css`。

新增 `tests/test_p2_pipeline.py`(13 条)。

### 界面:右栏按阶段折叠 · 检查分组 · Word 真预览 · 向导三开关 · 快捷键与命令面板 · 深色模式 · 品牌标

- **右栏按任务阶段折叠。** 六张卡在 940 高的窗口里要滚,但任何时刻真正要看的只有两三张:
  在写 → 进度 / 覆盖 / 已产出展开;出了 Word → 交付与出件前检查是主角,进度折成一行;停了 →
  先看停在哪;还没开始 → 参考资料展开。点标题可手动开合,手动记忆只在同一阶段内有效。
- **出件前检查分组。** 平铺清单混着三类动作,现在按「必办 / 建议 / 已通过」分组,每组一个主动作
  (必办 → 定向重做;建议 → 打开报告;已通过默认收起)。
- **Word 真预览。** 以前的「Word 视图」是把章节 md 套仿宋体 CSS 假装成 Word。现在
  `GET /v1/jobs/{id}/artifacts/{fn}/html` 用 python-docx 按 docx 真实内容出 HTML(标题层级、
  段落对齐、加粗/斜体、表格含合并格、内嵌图片、分页符),交付卡与结果页多一颗「预览」,
  右栏里的 docx 也能点开;页眉页脚与实际分页仍以 Word 为准,预览页脚写明。
- **新建向导第二步不再甩 20 行提示词。** 折成三个开关:「小标题按本章现拟」「逐条应答招标文件」
  「不编造」,全开时生成的文案与经典预填逐字一致(core-smoke 钉住);关一条就少一条并重排序号;
  全文在「高级」里,改过就以改的为准,可一键恢复。
- **快捷键与命令面板。** ⌘/Ctrl+K 打开命令面板:打字切任务、开检查/覆盖/日志、切外观与字号;
  ⌘N 新建任务;⌘⏎ 在输入框、修改结果、单章重写里直接发送/提交。
- **深色模式。** 跟随系统 / 浅色 / 深色三档(命令面板里切,记在本机);AntD 走 darkAlgorithm,
  自有样式的 40 余处颜色字面量全部收成令牌,Word 预览保持白纸黑字。
- **「待处理」独立色阶。** 出了 Word 但没过门禁的任务,徽章与侧栏圆点用紫色,不再和
  「需要你确认」的琥珀撞色。
- **字阶 11 档收成 5 档**(xs/sm/md/lg/xl + 展示字号),并可整体缩放(字号:小 / 标准 / 大)。
- **品牌标。** 一页折了角的标书加一枚勾,26px 下也认得出;侧栏、favicon 与桌面应用图标统一。

新增 `tests/test_docx_preview.py`(3 条)、`tests/next/check_p3_ui.spec.js`(7 条),核心冒烟 +5。

## [0.21.1] - 2026-09-01

### 官网的在线体验换成新界面

`site/app/index.html` 与 `site/demo.html` 过去是经典单文件前端的**两份逐字副本**,
靠 `check_frontend_stability` 的「三份必须完全一致」硬顶着。桌面端 0.21.0 已经换成
app-next,再留手工副本只会和它越漂越远。

- 在线体验(`/app/`)改为 **app-next 的构建产物,部署时现做**,不进仓库;Vercel 与
  GitHub Pages 两条部署路径都加了构建步骤,Pages 的触发路径也补上 `app-next/**`
  ——否则改了界面官网不会重新部署。
- `site/demo.html` 改为跳转页,老链接照旧可用。
- 「三拷贝一致」那条断言换了职责:现在守的是「产物是现做的」——`site/app/` 必须被
  .gitignore 挡住、两条部署路径都得构建、跳转页不许再变回整份副本。
- 署名断言跟着前端的真实构成走:改为盯 `app-next/src/App.jsx` 与经典前端两份,
  跳转页只要求它真的把人送到署了名的那一份。


### 修复:模型复读循环不再悄悄进 Word

一线实测的一份 v0.21.0 成品(10 万字)第五章开头整段是「…5 章节。 5 章节。 5 章节。…」——
「5 章节」连着重复 946 遍、整段 5781 字,而且这一整段挂在**章标题**的样式上。三层门禁
全都没拦住,原因各不相同:

- `doc_quality.detect` 的重复检测用 Counter 数「段与段之间」的重复,整个循环落在同一段里,
  于是 max_repeat=1,报「未发现异常」;
- 就算数对了,读 docx 时标题行会被加上 `#`,而检测的正文集合把 `#` 开头的行过滤掉,
  复读挂在标题上正好躲过扫描;
- 流水线那层只有 min_chars 长度门槛——卡进循环的模型字数从来不缺,长度门槛反而在奖励复读。

现在:

- 新增段内复读检测,并在**章节写完那一刻**就拦住(`validate_outputs`),该章按既有重试预算
  自动重跑,不再带着废稿往下走。门槛定在 1200 字:短复读本来就会被长度门槛拦下。
- 出件前检查会指名道姓说是哪一章复读了多少遍,首选动作是**重做这一章**,「仅折叠复读」降为
  次选——折叠只能把几百遍变成一遍,折完这一章仍旧没有内容,不能让它看着干净就交出去。
- 内容门禁现在也扫章节稿,否则只看合并后的整册,说不出该重做哪一章。


## [0.21.0] - 2026-09-01

### 界面重构:React + Ant Design

主界面从单文件前端整体迁到 React + Ant Design(含 Ant Design X 的
ThoughtChain / Sender / Prompts)。做法不是重写而是**机械抽取**:状态机、
SSE 断线重连、事件归约、回放保护、幂等键、文案翻译层从旧源码逐字搬进
`app-next/src/core/gen/`,冒烟测试第一条就是「抽取产物与基准源码逐字一致」。
功能点以上一版为基准冻结成迁移契约(`app-next/PARITY.md`),同一套浏览器
契约跑两个界面:新界面 41/41、经典 29/29。

- 执行过程、标书大纲、章节撰写、评分点覆盖都换成真正的组件;右栏新增
  「评分点覆盖 · 实时」环形卡、章节完成度进度条、出件前检查的必办/建议标签。
- **对话自动跟到最新,用户上翻读历史时不打断**;**工作日志限高可滚并贴底**,
  几百行台词不再把对话顶到几千像素以外。这两条是迁移过程中丢掉又补回的
  经典行为,已经钉进 `tests/next/check_reading_flow.spec.js`。
- 桌面壳的 `frontendDist` 指向 `app-next/dist`;经典界面(`app/src/index.html`)
  保留一个版本作回退。

### 评分点漏项按章一次补齐

未覆盖的评分点按章节分组,组头一次把该章所有漏项拼进同一条补充要求下发。
此前只能一条一条点:引擎重写时会把整单锁成 running,第二条必然被 admission
挡回来,而且后一次重写会盖掉前一次的稿子——所以按章分组不只是省事,是唯一
正确的批法。每条带勾选框可摘,派发后面板不关,并明说「引擎一次只重写一章」。
单章重写补充要求的上限相应从 500 字提到 2000
(`generation_pipeline.REWRITE_NOTE_MAX`):覆盖表单条最长近 300 字,
500 装不下两条整批,而截断的补充要求比没有更危险。

### 出了整册 Word 不再被判「未完成」

交付稿在手、只差几项提交前处理的任务改判「待处理」(琥珀),副标题先说
「Word 已生成」再说还差几项;出件前检查一条说清标题和怎么补,不再把中文标题
按空格切碎拼成读不懂的片段。覆盖仪表的 title 汇总未覆盖原因,鼠标一停就知道
该往哪使劲。

### 查更新不再依赖 GitHub 可达

更新清单改为由 CI 在发布成功后提交成静态文件(`site/updater/*.json`),
由 Vercel 200 直出。此前的 rewrite 转发方案实测没达到目的:GitHub 的
`/releases/download/…` 会 302 到 release-assets.githubusercontent.com,
Vercel 把 302 原样透传,客户端仍然必须连上 GitHub。注意范围:清单里的下载
地址仍指向 GitHub Releases,**装更新**依然需要 GitHub 可达。

### 引擎

- 回环 Origin 白名单放宽到 `*.localhost` 整族(RFC 6761 恒为回环):ES module
  按规范一律带 Origin 头,`tauri.localhost:端口` 的静态资源请求会撞 origin 闸。
  新增两条安全钉子,原有恶意域拦截不动。

回归:pytest 398 通过;新界面浏览器契约 41/41、经典 29/29;核心状态机无头
冒烟 18/18;前端源码检查全过。


## [0.20.5] - 2026-08-27

### 界面重心:从「流程」转向「交付物」(P0 第一批)

- **标书大纲成为任务主视图**:中栏新增「标书大纲 / 执行过程 / 对话与要求」页签,分段生成任务默认打开大纲——每章一行,状态点(排队/在写/完成/未完成)+ 实时字数,点章节即预览;流程台与对话降为二级页签,原功能不变。
- **单章重写**:已完成或失败的章节可以只重做这一章,并附补充要求;其余章节锁定不动,旧稿存入任务目录「历史版本/」可随时找回。机制上走执行契约摘要:重写标记改变本章摘要,汇总/质检/最终 Word 因摘要联动自动重做,不需要整单重跑。新端点 `POST /v1/jobs/{id}/chapters/{node}/rewrite`。
- **评分点覆盖仪表**:顶栏常驻「评分点覆盖 M/N」,口径 = 规划无缺口 + 落到具体章节 + 该章节已写完;点开列出未覆盖项,已定位到章节的评分点可一键「补写应答」(复用单章重写,不动其他章节)。新端点 `GET /v1/jobs/{id}/coverage`,数据源是本就生成的《评分点响应矩阵.md》。
- **解析确认门**:新建任务默认开启「开始撰写前,先确认解析出的关键信息」。响应规划完成后暂停,以结构化卡片展示本地确定性提取的项目名、递交截止、资质要求、评分办法与废标风险条数;确认即继续,修正会写入「你的要求.md」并触发响应规划按修正重新核对——读错了当场改,不用跑完 30 分钟才发现。向导第三步可关闭;对已有任务和旧版任务无影响。
- **报错横幅三段式**:所有错误/完成横幅统一为「发生了什么 → 内容是否保留 → 一个主按钮 + 一个次按钮」,其余动作收进「更多」;不再出现五颗按钮并排让人不知道点哪个。
- **Word 样式预览**:预览抽屉对 md 产物新增「Word 视图」(宋体、居中标题、实线表格,导出同款版式示意),默认开启,可切回原文;底部注明最终以导出 Word 为准。

### 进度可信与「生成了 Word 还报错」修复(试用反馈第二轮)

- **进度回放不再被钳回「检查资料 1/12」**:事件回放的防伪造证据钳制过去只认智能体模式的产物文件(图片索引、格式规范等),分段流水线不产这些文件,证据链在缺口处截断,真实进度被整体拉低。现在流水线节点状态(引擎亲手写盘)直接换算为 12 步证据,回放进度与实际执行一致。
- **Word 格式门禁从「一票否决」改为「如实分级」**:Word 能打开、检查能跑完、只是样式项不符 = warn——文件照常交付,不符项进「出件前检查」逐条人工确认;只有检查跑不成/文件损坏才是 fail。此前样式不符会把 word_export 节点打失败、把 settle 判停,用户看到的就是「Word 明明在文件夹里,应用却说任务失败」。
- **格式报告过期(stale)自愈**:质检/手动重试重建 Word 后旧报告失效,现在出件检查现场重跑一次格式检查并绑定新文件字节,不再因为报告旧就判红;「不能沿用旧绿色结论」的防护语义保持不变。
- **等待确认 = 停表,不是失败**:等待解析确认时右栏显示「⏸ 等待你确认 · 不计时」,不再跑「约剩~39 分 59 秒」倒计时,也不会误挂「没出 Word,未完成」红牌;确认门同时落一个可续跑的明确停止态,应用重启后状态依旧正确。
- **估时只给分钟粒度**:超过两分钟的预计剩余显示「约剩 ~N 分钟」,不再显示到秒的假精度。

### 自动更新与版本治理(商业化基建第一块)

- 桌面端集成 Tauri updater:应用内「立即更新」一键完成签名校验→下载→安装→自动重启;清单与更新包经 minisign 私钥签名,公钥固化在应用里,篡改即校验失败。更新端点自有域名优先、GitHub Release 兜底。
- 新增发布策略文件 `release_policy.json`(自有域名托管,引擎每 6 小时拉取并本地缓存):远程宣布 latest / minimum_supported / sunset,可分级推动升级(提示 → 强提示 → 过期停开新单)。
- 版本门禁:过期版本拦「开新单/重跑/重做」(HTTP 426 + 持久横幅带「立即更新」);续跑、单章重写、查看与导出永不受限——已经开始的活永远允许收尾。每个安装包自带约 90 天构建级落日,联网即被策略自动续期,离线囤旧版无法长期开新单。`BIDDOG_NO_SUNSET=1` 为运维逃生门。
- CI:配置 `TAURI_SIGNING_PRIVATE_KEY(_PASSWORD)` 后自动产出签名更新包与 `latest.json` 并附到 Release;未配置时仅跳过更新产物,安装包构建不受影响。
- 详见 `docs/自动更新与版本治理.md`(含发版清单、密钥管理与未来 license 衔接)。

### 配套

- 新增回归:`tests/test_p0_features.py`(覆盖度口径、单章重写含归档/契约/并发拒绝、确认门与修正闭环、建单标志)、`tests/test_progress_trust.py`(流水线检查点换算、回放钳制、格式分级、stale 自愈、等待终态清理)与 `tests/check_p0_ui.spec.js`(大纲页签、覆盖仪表、重写弹窗、确认卡、等待停表的真实浏览器交互)。
- 受限环境可用 `BIDDOG_CHROMIUM` 指向系统 Chromium 跑浏览器测试;不设置时行为与之前一致。

## [0.20.4] - 2026-08-26

### 生成稳定性专项(针对一线实测反馈:六次生成一次成功)

- 流水线模型节点改为流式请求:以「空闲 90 秒」替代「总时长 240 秒」判死。非流式长请求要等整段生成完才有首字节,国内中转网关普遍 60-100 秒空闲即掐连接,章节动辄 2-4 分钟必然被掐——这是频繁「当前节点连接中断」的最大元凶。断流时保留已生成内容按截断续写,不再从头再来;网关不支持流式自动降级。
- OpenCode 会话模式新增第三路心跳(消息体活动签名):SSE 半开、长章节暂不写文件时不再被误判为卡死并被亲手打断;SSE 半开检测从 600 秒收紧到 180 秒;自动恢复的连败预算随真实进展清零,整单上限 12 次(旧版整单只给 2 次)。
- 节点重试预算 3 次→5 次;连接/限流类错误按 8s→24s→72s→216s 分钟级退避,等得起一次真实的网络恢复;退避等待可被「停止」秒级打断。
- 引擎启动对账:上次异常退出(崩溃/睡眠/强关)留下的「运行中」孤儿任务自动归位到检查点并给出「从已保存内容继续」,不再显示「任务意外中断」死局。
- 「这一单正在跑,不用重复重试」死锁修复:worker 线程异常死亡后的僵尸占位自动回收;同节点重复点击重试返回「已在进行中」而非报错。
- 同网关更换 Key 不再作废检查点(旧版用户换 Key 自救反而被迫从头重来);网关变化仍硬拦截。
- Windows 上用户用 Word/WPS 打开中间产物导致的文件锁(PermissionError)改为可重试,提示关闭文件后重试,不再把整单打死;Word 导出节点同样允许重试。
- 素材超过节点安全上限(1000 个文件/100MB)改为截断+诊断记录,不再直接异常终止——旧版素材库养得越大任务越必然失败;全局素材合并跳过「原始标书」大文件原件。

### Word 导出失败修复(Package not found at _MEI...)

- 根因:PyInstaller onefile 把 python-docx 模板解压到系统临时目录,Windows 磁盘清理在引擎跨夜运行后把它删掉。三层修复:桌面壳启动引擎时把 TMP/TEMP 指到数据目录受控子目录(系统清理碰不到,顺带清扫 7 天以上崩溃残留);引擎启动时备份模板到数据目录并经 BIDDOG_DOCX_TEMPLATE 下发;技能包导出脚本在模板缺失时自动改用备份。
- 「立即补出 Word」失败时按真实原因返回(模板缺失给出「重启应用即可恢复」指引),不再误报「没有找到正文稿请重跑」。

### 图片打标修复(合同截图类 100% 失败)

- 视觉打标 max_tokens 500→1000:合同截图 OCR 文本长,输出在 500 token 处被截断,JSON 停在字符串中间(报错列号 520/539 即截断点)。
- JSON 解析全面容错:转义裸控制字符、补关截断字符串、逐步砍尾部残缺键值对;解析失败自动带更严格指令重试一次;仍失败降级登记(类别=其他、图注=文件名)不丢图,后续可一键重新打标。
- 入库出现新图片且已配视觉模型时自动开始打标,无需手动点按钮;打标启动加锁防止双击并发双开。

### 素材与格式支持(反馈 1/2/3/7)

- 素材库入库支持 PDF(文字层提取+编号行自动升标题拆章,扫描件给出明确指引)、.doc(魔数三分流:伪 doc 直解/RTF 本地提取/真 OLE 借 Word/WPS/LibreOffice/textutil 转换)、HTML 与图片直接入库。
- 建任务导入的材料自动回流素材库「任务导入」分类(md5 去重,可用 save_to_assets=0 关闭),下一单直接复用;流水线素材读取同步支持 .doc/.html。
- 任务详情展示「本单导入素材」与「参考资料」两组清单,创建时导入的文件不再凭空消失。
- 修复:无素材任务上传参考资料会被写进全局素材库并串到之后所有任务(跨项目串档隐患)。

### 诊断与状态真实化(反馈 5/8)

- 一键诊断新增第 4 项「本单执行」:任务失败时不再三项全绿误导;新增人话结论与建议动作;「技术详情」输出可读文本而非原始 JSON。
- 「查看运行日志」在无文本日志的运行方式下由事件流+诊断流水合成带时间戳的可读时间线,不再显示「当前运行方式没有文本日志」。
- 「模型连接」检查项随真实连接成功/断开/自动恢复实时更新;节点生成期间每 30 秒心跳,超过 3 分钟的正常长生成不再被标成「响应偏慢像中断」。
- 任务终态按真实原因归因(连接中断/上游限流/输出未通过),不再一律写「连接中断」。
- 前端不再对历史错误事件回放弹「任务没有按预期继续」横幅(运行中/已完成任务重开被误报的根源);新进展自动清除横幅。

### 工程与安全

- CI 构建依赖锁定版本范围(requirements-build.txt),消除 PyInstaller/fastapi 漂移;spec 关闭 UPX(杀软误报重灾区)与 CI 保持一致。
- /v1/assets 上传接口修复路径穿越(category/filename 未净化可写任意本地路径)。
- 任务事件 SSE 改为按字节偏移增量读取,长任务多标签页不再每秒全量重读事件文件;连接寿命到期显式通知前端重连。
- doc_quality 解析 WPS/.doc 转存 docx 时 p.style 为 None 不再崩溃(与引擎侧同坑同修)。

## [0.20.3] - 2026-08-23

### Word 表格可读性与格式门禁

- 表格单元格显式覆盖正文样式：数值缩进与字符缩进均强制为 0，段前段后为 0，行距固定为 1.15 倍，不再继承正文的两字符首行缩进和 1.5 倍行距；兼容 Word、WPS 和 LibreOffice 的不同继承逻辑。
- 列宽改为按“序号/状态短列、姓名/日期紧凑列、要求/响应/证明材料叙述列”语义分配；叙述列超出竖版版心时自动切换横版，避免中文一字一行。
- 表头和所有数据单元格统一左对齐，不再根据序号、状态或内容长度自动居中；表头仍保持加粗和跨页重复。
- Word 格式自检新增“表格·段落”和“表格·对齐”硬门禁；任一单元格重新继承正文缩进、行距或被改为非左对齐时，禁止交付 Word。

## [0.20.2] - 2026-08-22

### 桌面端本地引擎自修复与离线反馈修复

- 桌面应用新增不依赖本地 Python 服务的引擎修复命令；端口被旧版中标狗引擎占用时，会核验进程身份和可执行路径后安全关闭旧进程并启动安装包内新版。
- 旧引擎无响应时先尝试正常结束，再次核验进程路径后才升级为强制结束；未知或非中标狗进程绝不会被关闭。
- 引擎健康检查加入 900 毫秒上限，避免端口能连接但不返回数据时界面长期停在“连接引擎中”。
- “一键诊断”在桌面端升级为“检查并修复”，可直接处理旧进程和端口占用；普通任务诊断仍保留原行为，不会被误判成引擎修复。
- 引擎启动增加互斥保护和 30 秒恢复等待，避免后台启动与人工修复同时拉起两个实例；恢复成功后自动清除首页离线提示并继续任务连接。
- macOS 与 Windows 都按安装包内 `bid-engine` 的真实路径识别可接管进程；路径无法验证时只给出诊断，不执行破坏性操作。

## [0.20.1] - 2026-08-22

### 长文节点续写、安装环境与流程可视化稳定性修复

- 招标文件解析改为本地确定性提取，评分点、废标项与响应规划才进入模型节点；避免 Agent 反复读文件、长连接停顿和整个解析阶段重跑。
- 长章节达到单次输出容量时保留已生成内容，从最后完整句续写一次；不再丢弃整章重写。分章按短节点独立落盘，中断只重试当前节点。
- 默认极速模式使用已实测的 DeepSeek 快速模型；标准模式仅把最终复核交给质量模型。Word 汇总、格式审核和交付门禁保持本地确定性执行。
- 安装包补齐内置 Skill ZIP 和 PyInstaller 规格；首次启动会从安装载荷自动解压写作规则与 Word 导出脚本，不依赖 SoWork 或客户机器上的开发目录。
- 任务取消、节点晋升与旧流水线迁移加入原子检查；已取消的迟到结果不会被误交付，旧质检报告也不会被冒充为新的模型复核证据。
- 六阶段流程台新增完整节点详情：当前动作、阶段产物、实际/预计耗时、断点、恢复、重试和错误信息均可展开阅读；长文案自动换行，窄屏改为单列子节点。
- 本地验收覆盖 355 项 Python（跳过 1 项 Windows 专属元数据检查）、46 项前端逻辑、11 项 Chromium 交互和 18 项 Rust；真实模型全链路 14 分 41 秒完成 17/17 节点、0 次重试。

## [0.20.0] - 2026-08-21

### 可恢复分段生成与真实交付门禁

- 默认 OpenCode 生成改为中标狗引擎主导的短节点流水线：本地解析、响应规划、分章撰写、偏离表、汇总、质检和 Word 各自落检查点；网络中断只重试当前节点，刷新或重启不会重写已完成章节。
- 模型节点使用独立尝试目录，只复制声明输入，默认禁止 shell、子 Agent、联网和目录外访问；临时输出验证通过后才原子提升为正式产物，招标文件中的指令不能改变系统边界。
- 极速模式继续作为默认；任务只冻结连接测试已确认存在的模型。上游 429/额度限制、网络断流和本地引擎故障分开提示，不再笼统显示“稳定模式”。
- 自动模板推荐的截断预览不再冒充完整招标解析；DOCX、PDF、TXT 增加大小、页数、字符和压缩包复杂度预算。
- 响应规划新增可校验 JSON 合同；技术、商务偏离表必须包含实际表格行。最终状态只有在有效 DOCX、真实目录、两类偏离表、内容质检和 Word 格式自检全部通过后才变为完成。
- 新增“重试当前节点”接口与按钮；普通继续不能绕过最大重试次数，人工重试也不会复用旧尝试目录。
- 验证覆盖 332 项 Python（macOS 跳过 1 项 Windows 原生元数据检查）、46 项前端逻辑、10 项 Chromium、18 项 Rust，以及一条离线真实 DOCX 交付链路和 8 组格式导出/拦截闭环；OpenCode 1.18.18 服务合同通过，真实网关冒烟明确识别到上游 429 限流。

## [0.19.9] - 2026-08-21

### 内置 OpenCode 稳定性升级

- 内置 OpenCode 执行外壳从 1.18.13 升级到 1.18.18，纳入上游流中错误保留、更多瞬时网络重试和重试风暴限制修复。
- Windows 安装包继续内置 x64 baseline 变体，兼容不支持 AVX2 的旧 CPU；CI 在安装完成后实际执行 `opencode-cli --version`，版本不是 1.18.18 则禁止发布。
- 保持现有会话、SSE 事件、暂停与断线续做协议不变；本次不改造招标解析或模型路由。

## [0.19.8] - 2026-08-21

### 慢模型流式提速与自动续做

- 模型中转不再用 `read(8192)` 等待流式响应攒满 8KB；改为逐个可用数据块立即透传，避免模型已有输出却被本地缓冲成“长时间无响应”。
- `stream idle timeout`、连接重置和同类瞬时断流会在原持久会话内最多自动续做两次；续做前先检查落盘产物，只补缺件，不从头重写章节。
- 自动续做过程直接显示在任务消息与运行状态中；只有两次恢复都失败后才安全停止或进入原有兼容路径。
- 当前实测连接的短请求中，极速模型首字节约 0.66 秒、标准模型约 3.84 秒；新任务继续默认使用极速模式，关键终稿可再切标准模式重跑。

## [0.19.7] - 2026-08-21

### 流程反馈与接口稳定性修复

- 流程台将“当前正在做什么”与“连续验收点”分开：章节已经落盘时会显示“并行撰写”，不再因为前置收据缺失一直停在“招标解析”。
- 分章过程稿不再被误认成完整正文；连接中断时明确显示已保留的章节数量，并提供“从已保存内容继续”，不再误报 Word 导出失败。
- 兼容纠正旧任务历史中的错误结论，升级后无需重跑即可看到真实阶段和恢复操作；运行日志中的 ANSI 控制码不再进入界面。
- 将流空闲超时归类为模型连接中断，保留已有内容与会话恢复入口。
- 模型接入网络探测移出配置锁和主事件循环，慢连接不再冻结健康检查、进度刷新和设置页面。
- 新建任务增加幂等键，网络重试不会重复建单；前端请求增加分级超时、网络错误说明与请求追踪编号。
- `/v1/health` 改为纯只读检查，桌面壳通过独立接管接口恢复同版本引擎，避免普通轮询意外改变退出状态。

## [0.19.6] - 2026-08-21

### 会话全生命周期修复

- 引擎或应用异常退出后，已落盘的会话会显示“继续做”，不再只能从头重跑。
- 暂停任务可以正式停止并释放模型切换锁；旧会话不允许跨模型、网关、Key、协议或 TLS 配置续写。
- 停止请求在启动预检、定向重做准备或续做连接期间到达时，未派发的 worker 不会在迟到响应后又被启动。
- OpenCode 重连同时兼容“完整历史重放”和“只发新事件”，断线后的真实新进度不再被当成旧消息丢弃。
- 事件日志并发写入加锁，SSE 只消费已完整换行的 JSON，并由服务端返回精确 cursor；损坏尾行、日志重建和重连不再造成永久漏事件。
- 重跑操作增加会话幂等键，双击、网络重试或首次派发异常都不会生成重复/假任务。
- 回答模型问题失败时保留原问题供直接重试；开启新一轮生成时会关闭上一轮遗留问题，不串会话。

## [0.19.5] - 2026-08-21

### 解析进度、停止竞态与默认极速模式

- 任务进度会根据已落盘的解析、组成、格式、评分和废标风险产物自动前移；事件丢失时不再永久卡在“正在读招标文件”。
- 招标正文已提取但后续分析尚未落盘时，界面明确显示正在识别目录、条款、评分项和废标条件。
- 修复用户点击停止后，迟到的连接错误仍误报“已切换稳定通道继续”的竞态；停止意图现在始终优先。
- “稳定通道”明确说明仍使用同一模型和同一套要求，技术原因仍保留在诊断详情。
- 新环境和首次接入默认使用“极速”`deepseek-v4-flash`；“标准”改为明确的“质量优先”选项。
- 已停止的历史任务不再锁死全局模式；旧会话与当前模型不一致时禁止跨模型续写并引导重跑。

## [0.19.4] - 2026-08-21

### 引擎恢复提示自动收起

- 修复选中任务时，启动早期的“本地生成引擎没有启动”被错误记到任务名下、引擎随后连接成功仍残留的问题。
- 引擎启动提示现在始终属于系统状态；后台重连成功后会自动清除，不再让用户误以为内置引擎仍未启动。
- 新增浏览器回归测试，覆盖“已有选中任务 → 短暂离线提示 → 引擎恢复 → 提示消失”的完整状态链路。

## [0.19.3] - 2026-08-21

### 旧引擎安全自动接管

- 覆盖升级后若旧版中标狗引擎仍占用本地端口，桌面壳先校验产品身份，再请它优雅退出；旧任务仍在跑时等待收尾，端口释放后自动启动新版引擎。
- 同版本引擎直接复用；未知进程或身份验证失败时永不发送关闭请求，只等待端口自行释放，避免误关其他软件。
- 旧引擎退出期间明确显示“正在安全切换到新版”；模型接入不再继续提交并误报“设置没能保存”，Key 保留在输入框，自动连上新引擎后可继续。
- 本地壳新增同版复用、旧版退出、活跃任务延后交接、未知进程保护和真实 HTTP 健康探测回归测试。

## [0.19.2] - 2026-08-21

### 长会话卡死自恢复

- 正式任务的 OpenCode Server 会话同时监测会话事件和任务产物变化；持续偏慢时先明确提示，达到硬超时且尚未生成正文时自动切换一次稳定执行。
- 已有正文或已经回落过的任务绝不从头重放，而是安全停止并保留内容，避免重复章节和文件覆盖。
- 模型连接自检改为真实任务同款的 Server 启动与会话探测，不再用短命令成功代替长会话健康。

### 阶段时间与环境指引

- 六段流程台每一段显示实际用时、历史/参考耗时和预计剩余时间；慢步骤会直接显示正在持续检查连接。
- macOS 从磁盘映像或 App Translocation 临时路径运行时，预检会提醒先拖入「应用程序」；安装包仍通过可执行文件相对路径定位内置引擎。
- 用客户故障诊断包复放验证：原任务在招标解析阶段长会话静默，而非内置引擎缺失。

## [0.19.1] - 2026-08-21

### 模板从提示词升级为场景模板包

- 内置模板细分为政府采购、软件与信息化、设备与货物、工程施工、运营运维和咨询研究六类；每类同时包含目录、评分响应、标准表格、材料槽位、格式和质检规则。
- “自动推荐”会读取招标文件场景特征并选择模板，不再把 `auto` 当成模板 ID 后报“模板不存在”；推荐结果会说明命中依据和置信度。
- 支持上传自有优秀 Word、可提取文字的 PDF、Markdown 或文本标书，生成待审阅模板草稿。标题与表头会先泛化为章节和字段语义；完整设计思路可展开查看，名称和目录可修改，结构不足时禁止保存。
- 创建任务时把完整模板包编译进任务要求并冻结快照；招标文件原文始终高于模板，后续修改模板不会改变历史任务。
- 场景规则参考财政部政府采购规范和国家发展改革委标准招标文件；来源、发布机构和版本随模板保存。
- 自动推荐的预览与建任务共用主件和补充要求；DOCX 表格中的采购内容也参与场景识别。上传分析限制为 50MB，并对异常压缩 Word 做展开保护。

### Windows 首步、诊断和依赖自愈

- 任务过程页新增紧凑的“生成流程台”，把 12 个执行步骤归并为环境准备、招标解析、响应规划、并行撰写、Word 装配和交付质检六段；每段状态、当前动作和检查点都来自任务目录中的真实事件与产物证据。
- 事件流连续三次断开后自动进入每 4 秒一次的任务状态轮询，恢复连接后继续按原偏移读取并停止轮询；断线只改变连接提示，不会把后台任务误报为停止，也不会重复创建或重跑任务。
- 任务在读取招标文件前先进入“检查生成环境”阶段，持续显示任务保存、模板识别、组件检查、模型连接和文件读取，不再让准备过程全部挤在第 1 步里像卡死。
- 默认链路固定使用内置 OpenCode；缺失时由任务内自动补齐，不要求客户另装 SoWork、Node 或 Python。Mac/Windows 源码启动脚本也会在已有虚拟环境中检查并补装缺少的依赖。
- 一键诊断点击后立即显示检查状态并置于问题卡上层；诊断结果包含生成组件来源、自动修复进度、任务预检和最近活动。
- 桌面壳从应用可执行文件所在目录确定性查找 `bid-engine`，找不到或启动失败时记录实际检查路径与系统错误；前端明确显示“本地引擎未启动、无法生成真实文件”，不再静默降级为演示模式。

### Word 安装包出件门禁

- PyInstaller 显式收集 `python-docx` 的 XML 模板资源，修复安装包导出时报 `docx/templates/default-header.xml` 不存在。
- macOS DMG 和 Windows NSIS 的 CI 会从最终安装载荷启动引擎、创建正文并真实导出 `.docx`；缺少 Word 资源的安装包无法进入 Release。
- 自动、默认和空模板值统一走后端权威场景选择；推荐预览不再阻塞建任务，快速双击也只能创建一个任务。

## [0.19.0] - 2026-08-08

### 从“看任务跑”改成“拿成品继续改”

- 完成页把最终 Word 放在第一位，同时汇总目录、技术/商务偏离表和出件检查；有 Word 才能显示“已完成”，缺件或检查未通过统一显示“未完成”。
- 可以直接用一句话提出增删改、插入内容或格式调整；每次修改生成新版本并保留来源、父版本和旧成品，不覆盖上一稿。
- 新增政府采购、工程、服务三类内置模板；自定义模板会冻结创建时的配置，后续调整不会悄悄改变已经建立的任务。
- 新增项目归类、归档区和批量归档、恢复、重新生成、导出；批量导出只包含对用户有用的成品，部分失败会给出成功/失败数量和可继续处理的问题卡。

### 状态说人话，波动时系统自己接住

- 用户侧统一为“准备中、生成中、需要你确认、已完成、未完成”五种状态，并显示当前动作、最近活动、预计剩余时间、已用时和用量。
- 开始生成前 OpenCode 服务连接较慢时会自动改用稳定运行方式；已经开始输出后若连接中断则安全停住并保留内容。页面不再直接抛出“执行外壳、链路、探活”等内部词汇，技术详情集中到一键脱敏诊断中。
- 外壳事件流现在会被持续读取，避免进程已启动但页面一直等不到完整回复；失败探测使用短时缓存，切换 Key 或配置会立即重新检测。
- 重新生成会复制全部素材、参考资料和用户要求；归档、项目等产品元数据独立保存，任务运行中的旧状态不能把它们覆盖回去。

### 第一次使用、后台运行和桌面可靠性

- 首次使用缩成“粘贴 Key → 自动检测 → 上传招标文件”三步；同一份发放 Key 会原子配置生成模型、视觉模型和内置 OpenCode，失败时不留下半套配置。
- 关闭有任务运行的窗口会转入后台；需要补充、完成或未完成时使用系统通知，Dock/任务栏显示整体进度，网页端不会与桌面通知重复弹出。
- 第二次打开应用只会唤起已有窗口，不会再启动或关闭另一份本地引擎；只有主实例显式退出时才请求引擎安全收尾。
- macOS/Windows 安装包继续在 CI 中做最终包内的 OpenCode 和引擎启动验证；发布新增不可变标签检查和 `SHA256SUMS`。商业签名仅在整套证书与公证凭据齐全时启用，半套凭据会阻止发布而不是伪装成已签名。

## [0.18.2] - 2026-08-07

### 完成态只认正文 Word，不再相信模型自己报的 12/12

- CLI、OpenCode server、内置演示统一走 `settle()` 出件闸门；正文 Word 不存在时，任务只能停止，不能进入“已完成”。
- agent 阶段信号改写入独立声明流；引擎按 12 项磁盘证据逐步核验后才更新 canonical 事件。声称做完但产物没出现时会原地停住并说明。
- 只有解析版、清单或报告时保留真实进度，显示“生成中断”，附脱敏后的日志最后 8 行与查看日志/重跑操作。
- “没有 Word”成为出件检查最高优先级红牌；解析版和分析报告不再进入正文修复，也不会出现“一键修复解析版”的危险建议。

### 标准/极速断流不再静默，也不再把半截内容当成功

- 建连前的瞬时失败仍会自动重试；已输出内容后的断开明确返回失败，禁止整轮重放，避免重复正文或重复工具调用。
- 半截 tool-call 参数永远不会发出完成事件；OpenCode error/stall/cancel 不再继续走成功收尾。
- 模式切换会让常驻 OpenCode server 和探活缓存按配置指纹失效；运行中或可恢复的暂停任务禁止全局换模，每单同时保存不含 Key 的模型快照。
- 执行外壳派发后若中断，保留已写产物并明确停止，不再自动切 CLI 从头重放；极速模式需要稳定出件时可手动切回标准模式。
- 本次发版前用测试 Key 对 `senseaudio-s2` 与 `deepseek-v4-flash` 各做 3 次真实短流，两种模式均 3/3 完整收尾；Responses bridge 与 chat passthrough 两条本机中转路径也各自通过。

### 卡死、计时、诊断和升级链路补齐

- CLI 回落模式连续 15 分钟没有日志或产物变化会停止，3 小时总闸保留；修改已有正文会被识别为活动，控制文件轮询不会伪造活动。
- 页面改算活跃时长，相邻事件超过 10 分钟的部分列为“中途停顿未计入”；任务终止后计时不再继续增长。
- 每条任务可一键导出脱敏诊断包；包含事件、日志尾、任务/进度、系统版本、配置摘要和中转状态，Key 与本机令牌不会出包。
- 引擎后台 3 秒检查 GitHub 正式版，断网静默；确有新修复版时才在侧栏显示下载入口。
- 桌面引擎只接受 Tauri/本机可信来源；模型接入、执行命令、环境变量和中转状态均改为只写不读，诊断包按当前配置精确脱敏，不再把已存 Key 回传给页面。
- 定向重做在所有执行分支派发前冻结旧 Word 摘要；旧稿未变化时只能停止，不能复用旧 Word 误报完成。原生 Responses 缺省/显式非流式响应保持 JSON，不再被套成 SSE 后误报断流。
- 覆盖安装不再读取旧 WebView 缓存：桌面窗口使用 incognito 会话和带 `desktop=0.18.2-18802` 的版本入口，只连接 `127.0.0.1:18802` 并校验精确引擎版本；旧驻留进程不会再让新版 App 显示 v0.16.0。
- 启动时会迁移旧版 `~/Documents/标书助手` 数据，并把 `BID_HOME` 固定到 `~/Documents/中标狗`；历史任务、素材和日志沿用新目录，不再随启动位置漂移。
- 任务侧栏的「状态不明」与「已完成」分组默认收起且可持续折叠；新增批量管理、全选/取消全选和批量删除，单个删除失败不会中断其余任务，结束后统一汇总成功与失败数量。
- 技能包证据改为三态：只有当前 OpenCode 会话从首行到末行完整读取且由引擎签发本轮证据时显示「已验证使用」，日志不足或旧任务显示「暂无法验证」，只有明确未装载/未注入/未接收时才显示「未检测到」；不再把“没有拿到运行日志”误报成红色未使用，任务目录里的普通 JSON 也不能伪造已验证状态。
- 所有新建、重跑和定向重做统一初始化本轮 `run_id` 与技能派发收据，历史任务缺少新版结构化字段时只标为无法核验，不倒推成未使用。
- App/引擎重启后会从 OpenCode 的持久会话安全重放读取证据，并用本进程临时密钥重新签发；已验证状态不再因重启消失。开始、继续与定向重做会在拉起线程前原子占位，快速双击或并发请求不会启动两个 agent 交叉覆盖同一任务。
- 回归测试与 CI 出包闸门进入仓库：离线 Python 140 项、前端 Node 稳定性 20 项及 Chromium 真实点击 2 项，合计 162 项全部通过后，才允许构建 dmg/exe。
- macOS CI 会从最终 DMG 内实际启动 PyInstaller 引擎并检查 `/v1/health`；签名结构正常但 sidecar 无法启动的包不能发布。
- Windows CI 会静默安装最终 NSIS 包，再从安装目录实际运行 baseline OpenCode 和 `bid-engine.exe`，并核对 `/v1/health` 为 v0.18.2；只编译成功但安装后不能运行的 exe 不能发布。
- Windows 引擎日志统一切为 UTF-8；英文系统把 stdout 重定向到日志文件时，不再因中文启动信息触发 cp1252 编码异常而退出。
- 上述自动化回归与短流验证不等同于三份独立真实招标文件的严格出件验收，本版本不据此声称严格验收通过。

## [0.18.1] - 2026-08-07

### OpenCode 从「喂一条命令等它死」换成常驻 server：看得见、停得掉、问得着、崩了能续

以前跑一单是 `opencode run --auto`：喂一条 prompt、等进程退出。中间什么都看不见
（子 agent 的输出根本不走 stdout，运行日志停在 8 KB 不动）、停只能杀进程组、
agent 想问也问不着、崩了从头再来。这一版换成常驻 server + 单会话订阅：

- **看得见** —— 事件流转成真实台词（`读 素材库/公司介绍.md`、`执行 quality_gate.py`、
  `派子 agent 标书01-组成规划`）。「它正在做什么」第一次对 OpenCode 有内容；
- **停得掉** —— 停止走 `interrupt` 优雅中断，半截产物完整落盘。不再杀进程
  （server 是多任务共用的，杀了会把别的任务一起打死）；
- **问得着** —— agent 的提问推到界面，答案经 `/question/{id}/reply` 真的回到它的上下文；
- **崩了能续** —— 会话 id 落在 `任务.json`，重开应用凭它接着做；
- server 起不来或链路不通会**自动回落**到原来的命令行模式，不会因此跑不了单。

### 修掉一个会让整单白跑的判定错误

真机第一次跑 server 模式：agent 正在读素材、转招标文件，引擎却在 30 秒时宣布
「跑完了，但任务目录里没有生成任何交付物」并把任务停掉。

病根是 `GET /session/status` 在 OpenCode 1.18.13 上**恒返回 `{}`** —— 实测连续
150 秒每 10 秒取一次，15 次全空。拿它判「忙不忙」等于恒为「不忙」，
「连续 8 秒不忙就收工」于是一秒都不用等就成立。（接口文档里那个 `session.idle`
事件同样指望不上：这一版一次都不发。）

改成看消息列表的落定状态：`finish='tool-calls'` 是这段说完了、后面还要调工具，
`finish='stop'` 才是这一轮真收工；并且要求事件流同时静下来，
免得在排队的两轮之间踩空。另加卡死兜底：事件流 15 分钟毫无动静又不 finish，
当面说清并停下来，不再干等三小时。

同一处错误还废掉了连通性探活 —— 它在回复还没到的时候就去读消息，没看见报错
就宣布「通了」，**Key 是错的也照样通过**。一并修好。

这类判定不该靠真机偶然撞出来（上游一挂就测不了，一单还要跑十几分钟）。
新增一份照抄 1.18.13 实测行为的假 server（status 恒空、消息倒序、finish 三态、
真实事件名），几秒钟把整条判定链跑一遍，32 项断言。

### 暂停按钮不再骗人：改成「停下来 + 记住 + 接着做」

这是那张「界面承诺、链路没接上」表里最后一行。以前点暂停只是把标志位写进任务文件、
回一句「已暂停，随时继续」——而**只有演示引擎读那个标志位**。真跑的时候 agent
一个字都不知道，照写不误：界面说停了，它还在花钱、还在改文件。

真 agent 没有「挂起」这回事，能做到的是停下来再接上：

- **暂停** = 优雅中断这一轮，写到哪儿停在哪儿，产物完整落盘；
- **继续做** = 回**原来那个会话**往下接 —— 它还记得读过什么、写到哪，
  不会推翻已经写好的。这跟「重跑本任务」不是一回事：重跑是另起一单从头来。

配套：新增「已暂停」状态（与「已停止」分开）；能不能续由引擎逐单判断
（这一单有没有留下可用的会话），没有会话就不画那颗按钮——点下去必然失败的按钮
不如不出现；续不上时当面说清并指「重跑本任务」这条明路，**绝不悄悄改成从头重跑**
（那会把已有产物覆盖掉）；暂停没成功时如实报错，界面不画成已暂停。

### 断线重连不再把台词说两遍

事件流断了会自动重连（不然它一断台词就永久静音）。实测发现：**每次连上，
OpenCode 会把这个会话的历史整段重放**——断开重连，一模一样的 8 条事件又完整来一遍。
原来只去了相邻重复，拦不住这种：长任务每断一次台词就整段重复；更糟的是重放的
第一条派活会被当成第二条，对着用户说「收到你补充的要求，已排进队列」——他什么都没说。
现在按序号跳过已经消费过的。

### AI 提问时，界面上真的有地方能答了

两处以前是形式：

- agent 问开放式问题（没给选项）时，界面上一个能点的都没有，聊天框打字走的是
  「排队补充要求」那条路，根本回不到它的等待里。它一直等，用户也不知道在等什么；
- 给了选项时多挂一颗「我来输入」，点下去把「我来输入」四个字当答案发过去 ——
  agent 收到一句废话，比没有这颗按钮更糟。

现在：点「我来输入」是把输入框**切到回答通道**（有醒目提示条显示在答哪个问题，
可随时取消）；开放式提问自动切进去。回答走 `/answers` 真的回到 agent 上下文，
提要求仍走原来的排队通道，两条不串。真浏览器点了一遍，24 项断言。

### 后端说的话不再被界面吞掉

界面调引擎时，非 2xx 抛的是一串「/v1/jobs/x/resume 400」。于是引擎那句
「原来的会话已经不在了（执行外壳重装过或数据被清理），只能重跑本任务从头来」
死在这一步——用户看到状态码，真正该看的话被我们自己丢了。现在异常带上后端的原文。

### 顺手关掉一个本机口子

OpenCode server 不设密码时自己会警告 `server is unsecured`，而那不是空话：
这个 server 的 bash 与文件写入是全放行的，本机任何一个进程只要 POST 一下
就能以当前用户身份执行任意命令。现在每次起 server 现生成一把随机口令
（HTTP Basic），事件流也一并带上。

## [0.18.0] - 2026-08-06

### 默认引擎换成 OpenCode（拿真实招标文件跑出来的结论）

同一份真实招标文件（代码审核平台采购，18 条功能需求）、同一套素材、同一个模型
（senseaudio-s2）、同一份技能包、同一条提示词，**唯一变量是执行外壳**：

| | codex | **opencode** |
|---|---|---|
| 偏离表行数 | 36 | **112** |
| 应答密度（÷18 条功能需求） | 2.0x | **6.2x** |
| 《评标索引》 | **没出** | ✅ |
| 废标风险清单 | 0.7 KB | **19.7 KB** |
| 评分点响应矩阵 | 1.2 KB | **16.8 KB** |
| 正文字数 | 22,337 | **41,934** |
| 质检结论 | 有建议项 | **通过** |
| 模板化命中 | 0 | 0 |

**《评标索引》codex 根本没出** —— 技能包要求它排在整册最前面、评委按它逐项翻，
索引缺项 = 那项默认没响应，是直接丢分的。应答密度 2.0x vs 6.2x 也差了三倍。
两边模板化命中同为 0，说明 opencode 的字多是写透了，不是灌水。

结构性原因：opencode 有原生 task 子 agent，每章一个子 agent 独立写、各自有完整
上下文预算（实测日志里能直接看到「标书01-组成规划 / 02-格式提取 / 03-评分废标」
三个 Explore Agent 并行派出）；codex 在单一上下文里串行写完六章，越往后预算越紧
——它第 1 章只有 1,320 字。

改动：
- **「自动(默认)」= OpenCode**。存量用户的 `kind=s2` 自动切过去，不需要迁移；
- **codex 保留为显式可选项** —— 这个结论目前只有一单样本支撑，客户真撞上问题要能一键切回来；
- 自检合并成一条（s2 与 opencode 现在是同一个引擎），三层分层诊断保留；
- 一键安装执行外壳默认装 OpenCode（以前写死 codex：客户点完装回来一个用不上的壳，
  自检照样报缺外壳）；
- 安装包内置 OpenCode。**x64 一律打 baseline 变体** —— 打包时不可能知道客户 CPU
  支不支持 AVX2，而标准版在老 CPU 上是直接崩、报错还看不出是指令集问题。
  CI 打完当场跑 `--version` 验证，跑不起来就不发包。版本钉死 1.18.13 不跟随 latest。

### 顺带修的两处

- **`agent_env` 里 `kind=='s2'` 走的还是 codex 的环境准备**，于是默认引擎拿不到我们的
  provider 配置 —— 表现是 opencode 甩一句「UnknownError / Unexpected server error」，
  完全看不出是配置没送到。切换时实测撞到，已修。
- **配置生成失败不再静默吞掉**。原来是 `except Exception: pass`，出问题时客户只看到
  那句 UnknownError。现在把真实原因带出来，并在自检里翻译成「这是本机配置问题，
  不是网关或 Key 的问题，多为数据目录不可写」。

## [0.17.9] - 2026-08-06

### 参考资料真的送到 agent 手上（第五例「界面承诺、链路没接」）

用户传一份去年的中标标书想让它照着写，界面上**三处**都写着「AI 撰写时会参考它的写法」，
接口注释也写着「agent 与追问都会读到它」—— 而 `参考资料` 这三个字在 AGENT_PROMPT
与整个技能包里**一次都没出现过**。文件只是躺在任务目录里，外加往 `inbox.jsonl` 写一条
（那个文件同样没有任何消费者）。**agent 一个字都读不到。**

- 上传时把文件**复制进 materialsDir 下的 `参考资料/`** —— agent 被明令开工先
  `ls materialsDir`，这条通道是确定性的，不依赖任何模型自觉；
- 生成《参考资料清单.md》写清用法与禁令：**只作行文与口径参照，事实数据一律以素材库
  其余文件为准，不得从参考件里搬公司名/业绩/资质/人员/报价 —— 那是另一个项目的，
  搬过来就是编造**；
- 回执按真实情况分三档：任务已在跑就如实说「这一轮多半已经错过了，下次重跑或
  定向重做才会被读到」，不再承诺「会在后续章节里参考」。

### 「它正在做什么」不再全程空白

拿三个引擎跑同一份真实招标文件时量出来的：那块实时台词是**增量读 agent 的 stdout**
实现的，而三个引擎里只有 codex 真往 stdout 写 —— claude 的 `-p` 模式跑完才吐
（run.log 全程 0 字节）、opencode 的子 agent 输出走它自己的 SQLite 会话库
（run.log 停在 8KB 不动）。**三分之二的用户跑一个多小时，界面上一个字都没有。**

现在没有台词时降级显示「正在:分章撰写 · 第 7/12 步 · 已用 41 分钟」，并说明这个引擎
不往控制台输出台词、产物会在右侧「已产出」里逐个出现。有台词时照常显示台词。
状态不明（引擎被杀/断电）与暂存未开始一律不显示 —— 那两种情况装作还在跑就又是假承诺。

## [0.17.8] - 2026-08-06

### OpenCode 每一单都会永久挂死 —— 而且这解释了它为什么从没产出过一单

拿真实招标文件做 Codex vs OpenCode 对比时挖出来的。

现象：选 opencode 引擎跑一份真实招标文件，进度停在「预检 8%」不动。进程活着、
**CPU 0%**、没有报错、没有超时、日志不再增长。挂了 9 分半钟。它自己的日志里写着：

    message=asking  permission=external_directory  action.action=ask

素材库（materialsDir）与技能包（skillDir）**天生在工作目录（outDir）之外** ——
这是架构决定的，不是配置问题。opencode 对目录外访问默认 `action=ask`；而我们用的是
`run --auto` 非交互模式，**没有人能回答这个询问**，进程就停在那里。
三个前置分析子 agent 明明都已经跑完并返回了，主控一读素材库就撞上这道墙。

这是最难排查的一类故障：没报错、没超时、进度条不动，客户只会说「卡住了」。

修复：生成的 `opencode.json` 里补齐 `external_directory` / `read` / `glob` /
`grep` / `task` / `skill` 放行；`webfetch` 仍然关着（素材库是唯一事实来源，不许上网编）。
新增回归断言：**任何一项权限是 `ask` 都直接红牌** —— 非交互模式下 ask 就等于挂死。

**一条必须更正的旧记录**：v0.13.1 的 CHANGELOG 写着 OpenCode「11 项端到端实测通过」。
那 11 项测的是连通性、工具调用、协议直连，**没有一项跑过完整的标书流程** ——
完整流程才会去读工作目录之外的素材库，也才会撞上这道墙。**连通不等于能用。**
当年承诺的 Codex vs OpenCode 质量对比一直没有结论，大概率就是一跑就卡住、
没查出原因，然后被「界面精简」顺手砍掉了。

## [0.17.7] - 2026-08-06

### 界面上可点的东西,必须真的能用

客户报障:成品质检判红之后，面板里那颗「处理」点了毫无反应，卡在那儿不知道该干嘛。
查下来这一类有三处，写了个扫描器（从 CSS 收集所有 `cursor:pointer` 的 class，
比对事件绑定）一次挖出来：

1. 质检面板的「处理」——只是个 span，CSS 给了蓝色和手型光标，全文件没有一处监听它；
2. 聊天里的「定向重做不达标章节」——**最阴的一种**：引擎发的动作叫 `open_redo`，
   前端分发表里根本没这一项，点了走到函数末尾静默 return。看起来接了线，其实没有；
3. 输入框旁的麦克风——前端没绑事件，后端 `/v1/transcribe` 还是硬编码 501
   「本机未装转写模型」。**双重装饰，已撤下**：做不到的不摆在界面上。

改法是让后端说清「该怎么处理」，而不是让前端画个按钮糊弄：每条质检项自带可执行动作，
**没有动作的项一个按钮都不画**；质检抓到的每个具体红项直接挂「重做这一章」，
带着章节名走定向重做，不用人自己抄。

防复发：新增动作契约体检（引擎发出的动作词汇 vs 前端认识的，对不上直接红牌——
`open_redo` 和 `repair` 两个缺口就是它查出来的）、未知动作兜底（当面说
「这版应用还不认识这个操作，可能是引擎比应用新」，不再静默）、死控件扫描。

### 五条真机高频缺陷

- **暂存任务点「开始生成」100% 弹「启动失败，请重试」——而任务其实已经跑起来了。**
  一行代码调了个根本不存在的函数，抛异常被 catch 住走进失败分支。用户会反复点，
  最后多半去删任务重建，把正在跑的 agent 一起杀掉。
- **在向导里拖进任何一个公司文件，反而把整个素材库从生成链路上切断。**
  根因是一行二选一：任务目录只要有 `素材/`，全局库的公司介绍、产品能力表、
  资质与案例、应答要点、图片索引就全部失联，agent 满篇写〔需补充〕。
  **用户越认真传材料，产出越差。** 改为合并（同名以本单为准）。
- **任务刚开跑的几分钟到几十分钟，界面上没有停止按钮。** 它要等 agent 自己汇报
  第一条进度才出现，而真实 agent 读招标文件那段是不吭声的。拖错了文件想停，
  只能删掉整个任务或干等三小时超时。现在派发瞬间就有进度，按状态显示。
- **跑挂了跟正在跑长得一模一样。** 四种终态（手动停止/未能启动/没有产出/异常退出）
  的进度都是 0，而前端只有「≥100 就是完成」一个判据，于是全被当成「运行中 0%」
  一直转圈。现在由引擎给出五态结论（等你开始/进行中/已停止/状态不明/已完成），
  终止时保留跑到第几步，跑挂了也能定向重做。
- **关窗会杀死引擎，但杀不掉 agent。** agent 是独立进程组，于是它还在后台写文件，
  而负责收尾的代码（收拢产物、补出 Word、质检、完成播报）已经跟着引擎死了——
  关窗去开个会，回来是一个永远没有结局的任务。现在关窗改为请引擎自己收尾：
  没任务在跑就立刻退，有任务就跑完再退，中途重开应用会复用它。

### 老 CPU 装不上 opencode 外壳（会直接崩）

opencode 官方安装脚本会探测 AVX2，不支持就必须换 `-baseline` 变体；我们的平台映射
只有 AVX2 版本，老 CPU 客户机上二进制直接崩，报错还完全看不出是指令集的问题。
招投标客户里老工控机不少，这不是理论风险。已修，并联网核对：x64 三个平台的 baseline
都在，arm64 的全是 404（AVX2 是 x86 指令集）。顺带更正体积口径：文案写的「约 80MB」
实测是压缩 57MB / 解压 167MB。

## [0.17.6] - 2026-08-06

### 「跑完了,没有生成标书」——客户报障,已修

客户上传了一份招标文件,任务一路跑到 100%「完成」,右侧列着一串文件,
**就是没有 Word 标书**。查下来是两个各自独立的口子,都会导致同一个现象:

**口子一:正文写完了,最后一步没跑。** agent 把《投标文件_技术标.md》和自检报告都写出来了,
却没执行 `build_tender_docx.py`。而 md 文件本身也算交付物,于是「交付物列表」非空 →
任务照报「完成,已整理好 N 个文件」,「跑完却没产出」的告警根本不触发。引擎这边原本
只有在「质检发现问题并真的修了」时才会重建 docx,质检通过就直接放过去了。

- 现在:完成时若有正文稿却没有任何 docx,**引擎自己把 Word 出出来**——确定性脚本、零 token、
  不重跑,封面/目录/页眉页脚/图片落位都按格式规范生成,跑完在对话里说明是引擎补的。
- agent 异常退出、3 小时超时这两条路也一样补:崩在最后一步不该让整册正文陪葬。
- 万一连补都补不出来(比如技能包路径坏了),**播报不再说「任务完成」**,而是直说
  「跑完了,但没有生成最终 Word 标书」,并给「立即补出 Word / 看日志 / 重跑」三颗按钮。
- 正文识别放宽:原来只认以「投标」开头的文件名,模型把正文命名成《技术标.md》
  《XX项目标书.md》时,质检、重建 docx、缺件告警**三处一起失灵**。现在按常见命名识别,
  再兜底取最大的一份;报告/清单类文件名照旧排除。

**口子二:产物写进了子目录,没被收回来。** 指令里写着「不要另建下层输出目录」,模型照建不误
(`输出/最终交付/技术标.docx`)。原来的收拢逻辑只扫一层、而且文件名必须以「投标」开头,
够不着的就等于没产出——完成播报里列的「② 产物写到别处了」说的就是它,只是当时没真的去捞。
现在按目录深度递归收拢;`素材/`、`章节/`、**`参考资料/`(客户自己上传的输入件)** 一律不动,
根目录已有同名文件时以根目录那份为终稿,不覆盖。

**顺带堵住源头**:引擎指令和技能包 SKILL.md 都写明「**没有 .docx = 这一单没交付**」——
正文写完必须亲自跑导出脚本,`ls` 确认文件真在、体积不为 0 才允许结束;时间紧也要先出 Word。
并行流水线那条路里,出 Word 阶段返回 0 份 docx 时不再只记一句「0 份」,改为报硬失败。
技能包升到 v5.8,存量用户启动自动刷新。

## [0.17.5] - 2026-08-06

### 清掉灌水的残余燃料:引擎里还硬编码着「每章≥3500 字」

用本会话 6 次翻车做架构回放,6 挡 4;挡不住「模板灌水」那次的原因是
**燃料的真实载体不在我以为的地方**——v0.17.2 我只从前端预填提示词里删掉了字数目标,
但 `engine_v1.py` 的 AGENT_PROMPT 里还硬编码着「每章正文≥3500 字」,
还在告诉 agent「篇幅目标…逐条落实」;技能包 SKILL.md 里另有三处 ≥3500。
**只拔了一根管子,油箱还是满的。**

给定一个数字就是给了凑数的靶子:真机事故里给了「8—9 万字符」,弱模型直接靠复制模板
把 18 个章节套同一组小标题灌到 12 万字。现在全链路不再出现任何统一字数目标:

- 引擎 AGENT_PROMPT 改为「篇幅按招标文件的分量走」,并写明格式件/证明件/表单本来就短、不要往里灌;
- 《你的要求.md》条款去掉「篇幅目标」(客户自己写了字数要求才按它来);
- SKILL.md 三处固定值改为「字数验收只管论述章,拿同册其他论述章与招标分量作参照」;
- 演示台词里的「5 章全部 ≥3500」也改掉——演示本身不该教错的东西。

### 修 4 条工程标实测缺陷

- **docx 解析崩溃**(真实招标文件就崩):`p.style` 为 None 时无保护取 `.name`,
  WPS 出的、`.doc` 转来的 docx 常见。引擎与 `check_docx_format` 各加防护,三份真实文件实测通过。
- **废标条款检索失效**【废标级】:靠关键词找废标项,但实测一份工程招标文件里
  「★」「▲」「废标」「不予受理」**各 0 次**,整张《初步评审标准表》的闸门会被漏掉——
  初步评审不过是直接出局。改为按章节结构逐张表扫,关键词降为补充。
- **大纲被静默砍到 6 章**【废标级】:`multiagent_workflow.js` 一行 `slice(0, CAP)` 无条件截断,
  招标规定 11 项也照砍,日志只报「6 章并行撰写」——主控看不出丢了东西。
  格式驱动型不截断;真截断时把被丢弃的章名一并打出来。
- **拆解员「≤6 章」写死 + IT 专用合并配方**【废标级】:其中「资格+必备函件一章」会把
  投标函/身份证明/授权委托书/保证金/资格审查资料/项目管理机构六个必须分列的一级件揉成一章。
  改为按招标文件是否给了「投标文件格式」章分两支。

### 一条更正

上一版我说「工程标里投标报价占 100 分中的 50 分」——那个数字读自一份**虚拟案例**
(文件第 1 行写着「虚 拟 案 例」,全文 26 处标注虚拟)。**具体分值没有真实依据,撤回。**
技能包里「报价不代劳、列为人工必办项」这条仍然成立(那是常识层面的正确)。

真实样本盘点:招标文件 1 份(信创类)、招标+中标配对 1 对;**工程类的真实招标文件一份都没有**
(拿到的 4 份工程标全是投标文件)。所有工程类结论的样本基础要按这个打折看。

## [0.17.4] - 2026-08-06

### 拿真实工程标校准:4 份人写的工程标,我们的门禁原本 4/4 判红

投标人给了 1 份工程招标文件 + 4 份真实工程投标标书 + 1 对信创采购招投标文件。
把 4 份**人写的、实际投出去的**工程标喂进质检门禁,**全部判 RED**——门禁把行业标准做法当废稿。

- **格式件被当成"内容偏薄"**。工程标的投标文件由 10 项组成,只有「施工组织设计」是论述章,
  其余是格式件:法定代表人身份证明 163 字、授权委托书 269 字、主要材料单价表 29 字——本来就该短。
  豁免名单按 IT 标写的,全漏了。已扩充(投标函/身份证明/授权委托/保证金/单价表/工程量清单/
  资格审查/项目管理机构/其他材料…)。
- **「目 录」匹配不上**。中文标书爱把标题排成「目 录」「投 标 函」这种字间加空格的,
  正则 `目录` 直接失配,整条豁免形同虚设。标题匹配前先做空格归一化。
- **查重的判据本身错了**。参考标书里「调整作业时间提高机械利用率」等措施段重复 3 次——
  回原 docx 核对过,确实出现 3 次:同一套措施在"进度保证""工期保证"两章各复述一次是行业常态。
  实测**真实工程标重复率 8%,IT 灌水事故稿 9%**,占比这个指标根本区分不了。
  真正分得开的是模板化检测(同名小标题/同构表格):事故稿命中 2 条,4 份工程标全部干净。
  → 查重降为极端情况兜底(>18% 或单段 >8 次),判灌水交给模板化检测。
  修完:两份工程标转绿,事故稿仍红。
- 字数门禁加层级可靠性守卫:识别出的"章节"超过 30 个(多为三四级小节)时降为黄牌并说明原因。

### 按投标人实操修正:资质整块留位、截图只做技术响应

- **资质 / 业绩 / 合同 / 证照不再拆分成"一个资质一个小标题"**。这些材料公司手上都是现成扫描件,
  实际做法是**整块粘贴复制**,拆碎了反而没法贴。现在按招标规定的名称建一个章节,
  写清该放什么、对应招标哪条资格要求,然后留 `〔此处粘贴:…〕` 空白位。
- **这类图一张都不自动插**。实测参考标书 121 张图里 90 张以上是人员资质与合同页扫描件,
  自动插错一张就是造假风险。quality_gate 新增 PASTE_ONLY 判定:证照/合同/资质类只登记进
  《配图清单》标"留人工粘贴",不补插也不搬正;技术类图(平面布置/进度横道/工艺流程/产品界面)照常。
- **配图只做一件事:证明我方对某条技术要求或评分点的响应**。不为插图而插图。

### 偏离表是每份必做,应答索引放最前面

上一版我判断"工程标没有偏离表"——**错了**。甘肃农信信创采购招标文件第 15.3 条明写
「对本招标文件的技术、商务条款偏离表,供应商应据实说明响应或偏离情况」,并列为投标文件第 (9) 项;
对应的中标标书里也有。

- 商务和技术偏离表列为**每份标书必做**,漏了就是漏项。
- 新增《评标索引》并**排在整册最前面**(目录第 1 项)——实测真实中标标书就是这么做的,
  列结构照抄评分办法:`评分项 | 分值 | 评估标准 | 对应章节/页码`,每个评分点一行。
  评委按这张表逐项翻,索引缺项 = 那一项默认没响应。

### 还认清了两件事(写进 skill,但不是代码能解决的)

- **格式驱动 vs 内容驱动**是两类标书的根本分野。工程标的骨架由招标文件的「投标文件格式」章规定,
  照抄、一条不许增删改序(漏项即废标);IT 标才是自拟大纲。
- **报价我们碰不到**。实测招标文件里投标报价占 100 分中的 50 分,且「已标价工程量清单」是
  电子计价文件。已写死:报价列为补料清单头号人工必办项,**不许自动填任何价格**。

技能包 v5.7(内容更新);存量用户启动即自动刷新。
模板化门禁 24/24(含 4 份真实工程标防误伤基准 + 证照类不自动插用例)。

## [0.17.3] - 2026-08-05

### 通用性修正:章节结构跟招标文件走,不跟我们的习惯走

0.17.2 加的「技术方案 ≤6 章」是按 IT 服务类标书设的,**对工程类是有害的**——
工程标的招标文件常写明"施工组织设计应包括下列十项",硬砍到 6 章就是漏项,重则废标。改成:

- 招标文件**规定了**章节条目 → 照它建章,一条不许砍,**没有上限**;
- 招标文件**没规定** → 才 ≤6 章,靠写透而不是靠加章。

另外两条同源问题:

- **偏离表不再死绑「产品能力表」**。工程施工、咨询服务、货物代理这类非产品标书没有这个概念,
  素材库里没有能力表时改用应答要点 + 资质案例 + 公司介绍判定,判不了的写〔需补充〕——
  **绝不允许因为没有能力表就整张表填"响应"**,那是编造。
- **偏离表条数纪律**:先数招标文件里技术/商务条款各多少条,表就要有多少行,逐条对上。
  真实事故稿里技术偏离表只有 10 行、商务 9 行,而询比文件的条款是它的好几倍——
  这种表在评标现场等于没做应答。

技能包 v5.7(内容更新,版本号不变);存量用户启动即自动刷新。

## [0.17.2] - 2026-08-05

### 修:整册标书全是同一组小标题、同一张样板表

客户拿真实招标文件跑出来的 12 万字成品:20 章、每章下面都是同样 8 个二级标题
(需求与评分点映射/实施方法论/关键技术细节/交付物与验收口径/风险识别与应对/
服务流程与沟通机制/质量保证措施/人工补料与投标前复核),149 个二级标题只有 11 种;
58 张表里 54 张是同一个「控制项|响应安排|交付证据|责任边界」;
正文换个章节名重复同一段话,重复段占 19%。

- **根因是 v0.17.0 预填的那份默认提示词,不是 skill、也不是扩写环节。**
  那份提示词写了「技术上要有实施方法论、关键技术细节、交付物、风险识别与应对;
  服务上要有服务流程、SLA、沟通机制、质量保证体系」——本意是文档级清单,
  弱模型直接当成**逐章小标题模板**套满全书;再加上「8—9 万字符」的篇幅目标,
  它就靠复制模板灌到 12 万字(skill 明写技术方案 ≤6 章,实际出了 20 章)。
  这 8 个词在 skill 里一个都搜不到,逐条能对上的只有那份提示词。
- **重写默认提示词**:删掉全部名词清单与字数目标(篇幅目标正是灌水的驱动力),
  改成三条纪律打头——小标题必须每章现拟、逐条应答招标条款、不编造。
- **质检门禁加模板化检测**(原来这类稿子一路绿灯):同一小标题出现 >2 次判红、
  同一表头的表格 >4 张判红;重复段阈值 8→2(旧阈值要重复 9 次才判,事故稿最多 3 次,一条没拦住)。
  拿事故稿还原成 markdown 做基准:旧门禁 green/0 问题,新门禁 red/3 条。
- **skill 大纲纪律(v5.7)**:章数守住 ≤6;每章 must_cover 必须来自招标原文且各不相同;
  **严禁预设统一小标题模板**——通用名词清单一旦进 must_cover,撰写员就会套满全书。
- **资质单独成章**:《资质与业绩证明》下设 `公司及产品资质` / `项目团队人员资质` /
  `同类项目业绩`,每个资质单独一个三级标题,标题下先说明对应招标哪条资格要求,再贴证书图。
- **配图只在两处**:①功能应答(声明"支持"时配产品界面截图)②资质证书。
  方法论、流程、管理制度类段落一律不配图——凑图和凑字一样是灌水。

### 修:技能包改了却"没生效"

`server/` 下留着一份 CI 那步 `cp` 过来的旧同名 zip,`ensure_skill` 按候选顺序取第一个,
于是它抢在根目录的新包前面被解压。已删除该副本并加进 .gitignore;
`ensure_skill` 改成有多份同名 zip 时**按修改时间取最新**,这个坑不会再静默发生。

## [0.17.1] - 2026-08-05

### 修:跟 S2 网关的连接被掐断,整条任务就死

客户实测报回来的两条：

```
<urlopen error [Errno 54] Connection reset by peer>
<urlopen error [SSL: UNEXPECTED_EOF_WHILE_READING] EOF occurred in violation of protocol>
```

两条都带 `urlopen error`,说明是**连接建立/握手阶段**被对端掐断,不是传到一半。

- **根因**:`_upstream` / `_openai_req` 只在「证书校验失败」时换 CA 重试,
  连接被重置、握手 EOF、网关 5xx 一律直接放弃。中转层一失败就发 `response.failed`,
  codex 拿到后自己重连两次(等于把整轮重发一遍),两次都撞同一堵墙,任务就死了。
- **改法**:上游请求加瞬时故障重试,退避 0.8s / 2s / 4.5s。判定写得很严——
  连接重置、握手 EOF、`RemoteDisconnected`、网关 429/5xx 才重试;
  证书错误、401、404、DNS 解析失败一律秒失败,不白烧时间。
- **流到一半被掐**:以前已经吐出去的正文全丢、判整轮失败。现在如果有正文
  **且这一轮没有任何工具调用**,就按已收到的内容正常收尾,让 agent 接着走下一步;
  一旦掺了工具调用仍判失败——截断的 arguments 会让 agent 拿着半截 JSON 去执行,比失败更糟。
- **报错说人话**:不再甩 `<urlopen error [Errno 54]...>`,而是说明连接被掐断、
  已自动重试几次、已写出的章节都保留、点「重跑本任务」接着做,并给出三条排查
  (换手机热点验一下是不是本地网络设备在拦长连接 / 过几分钟再跑 / 切极速模式让单次请求更短)。
  「模型对话」那条 `回答失败:<urlopen error [SSL: ...]>` 同样翻成中文。

## [0.17.0] - 2026-08-05

### 修复:选了「极速」,任务却还在用上一个引擎跑

客户实测报回来的问题——快速接入卡里选了「极速(deepseek-v4-flash)」,任务一跑就退出,
日志里是 `No API key found for provider "sowork" … auth-profiles.json`。

- **根因**:「标准 / 极速」的分段按钮只改了说明文字,**从不写配置**——它要等用户再点一次
  「一键接入并测试」才落盘。历史配置里引擎还钉在 SoWork,于是"选了极速"其实什么都没换,
  任务用未登录的 SoWork 起跑,直接退出。
- **改法**:模式按钮点了就算数。已接过 Key 的,点一下立刻改绑并**回读校验**
  (kind / 模型 / Key 三项对不上就红字讲清,不再显示假的 ✓);没接过 Key 的,明说"要先粘 Key"。
- 快速接入卡新增一行**当前真正生效的引擎**;不是「自动」时直接点破"上面的标准/极速对它不起作用"。
- `saveAgent` 在引擎未连接时改为抛错——以前是静默 return,调用方会以为存好了。

### 「你的要求」现在真的会进指令,而且默认就有

- 以前这段文字只作为一条聊天消息记录下来,**并没有传给 agent**,但界面写着"会作为生成指令的一部分"。
  现在它会原样存成任务目录里的 **《你的要求.md》**;agent 提示词新增第 10 条:开工第一件事读它,
  优先级仅次于招标文件强制条款与格式门禁,并在《投标文件自检报告》里逐条回执(没落实的写明原因)。
- **默认预填一份投标方案专家提示词**:角色设定、8—9 万字符篇幅目标、逐条对齐评分点、
  技术/服务两个维度的展开要求、承诺函、不编造。可整段改写,旁边有「恢复默认」「清空」。
- 重跑任务会沿用同一份要求(否则第二遍产出和第一遍不是一个东西)。
- 《你的要求.md》是输入件,不计入交付物——否则"跑完却没产出"的告警会失灵。

### 外部引擎没登录时说人话

openclaw / claude / codex 的授权失效,不再甩一段英文日志加"异常退出(退出码 1)",
而是讲清是哪个引擎、为什么,并给「改用我们的 Key」「先用内置演示跑通流程」「查看运行日志」三个按钮。
「测试连接」里同样翻译。

### 官网上线 + 下载直链

- `site/` 双线部署:**Vercel**(根目录 `vercel.json`,Root Directory 保持仓库根)
  + **GitHub Pages**(`.github/workflows/pages.yml`,改 `site/**` 推 main 自动上线)。
- 官网「下载 macOS / Windows 版」改成**直指安装包的直链**,并按访客系统把对应的包排到第一个;
  打开页面时再问一次 Releases 接口,发了新版自动跟上——接口限流/离线则回落到写死的直链。
- Release 不再标 `prerelease`:标了的话 `/releases/latest` 与 `/releases/latest/download/…` 全部 404。
- 仓库补齐:`README` 重写、`BUILD.md` / `docs/Mac安装说明.md` 断链与过期文件名修正、
  `.gitignore` 增补(`*.orig` / `.env` / `preset_config.json` / `agent_settings.json`)。

## [0.16.0] - 2026-08-05

### 接入体验重做:用户只做两件事

- **快速接入 = 全部配好**:粘一串 Key + 选一个模式,生成引擎、对话模型、**识图模型**一次配齐(识图模型从网关模型列表自动挑,不用用户去高级里翻)。修一个真 bug:快速接入此前把视觉模型写成 `vision` 字段,引擎认的是 `vision_model` —— 所以识图模型一直没配上。
- **两种模式**:**标准(默认)** 用 `senseaudio-s2` 写正式标书;**极速** 用 `deepseek-v4-flash`,出稿快更省额度,适合试跑演示。命名刻意避开"质量/快速"的暗示——极速不等于写不好,只是取舍不同。网关里没有指定模型时按特征自动兜底(不会因为换了网关就配不上)。
- **生成模型可选了**:自动引擎面板里模型输入框变成真下拉,点「取模型」实拉网关全部模型(S2 / DeepSeek 等你套餐里有什么就能选什么),摘要行直接显示当前用哪个模型写标书。

### 「自动」引擎

- 引擎选项 `S2 模型 · 我们的 Key` → **「自动(默认)」**,并排到第一位:有 Key 就产真实标书,**没 Key 自动回落内置演示流程**并在对话里说明原因+给「去填 Key」按钮,不再空跑或报错。
- 高级区只留必要项:去掉「生成方式(多子 agent / Workflow)」下拉——用户无从判断,固定用更稳的多子 agent。

## [0.15.1] - 2026-08-05

### 修复

- **在线体验版的工作台词只有分隔线没有内容**:在线版是纯前端演示、不连引擎,自然收不到 run.log 台词。现在演示流程也走同一条展示通道,按 11 个阶段脚本化输出 60+ 行真实感台词(跑了什么脚本、写出什么文件、字数是否达标、图片搬正/补插、门禁核验结果),演示节奏相应放慢到每步 2.4 秒让台词跑完。
- 台词区标题去掉「流式」字样:运行中显示「⚙ 它正在做什么」,完成后「⚙ 工作过程回放 · N 行」。

## [0.15.0] - 2026-08-05

### 进度反馈(按"我不知道它在干嘛"的反馈重做)

- **流式工作台词**:对话区常驻一块实时控制台,把 agent 的真实动作逐行流出来(读了哪个文件、跑了什么脚本、写了什么产物),每步开头自动插一条阶段分隔线。运行中默认展开、自动跟随滚动;跑完自动折叠成「工作过程回放 · N 行」。引擎侧按增量读 run.log、过滤掉 session id/token 统计等噪声行,只留人看得懂的动作。
- **预计等待时间回来了,而且更准**:引擎记录每一步的真实耗时(EWMA 滚动平均,越用越准),前端按「当前步剩余 + 后续各步均值」估算——**第 1 步就能给出预计**,不再需要跑完两步才有数,也不会被某个超长步带偏。没有本机历史时用内置参考值并标注"约"。新增 `/v1/stats/stages` 查询每步均值。
- 演示流程同样产出工作台词,零配置就能看到这套反馈长什么样。

### 官网

- **首页截图墙换成真 Word 页面**:封面页、图文正文页(产品截图按锚点落在对应章节+连续图注)、技术应答偏离表页、章节图文页——全部由成品 docx 经 LibreOffice 渲染截取,未经修饰;删掉原来的应用界面截图(客户要看的是交付物,不是壳)。

## [0.14.1] - 2026-08-05

### 按真机反馈修复与再精简

- **存量配置迁移**:此前存过 `opencode` 引擎的用户,选项撤下后下拉会变空白、摘要显示裸值——启动时自动归并到 S2(Key/网关/模型全保留);未知引擎值落安全默认。前端对任何缺失选项兜底,绝不空白。
- **SoWork 本机故障翻译**:openclaw 的 `mismatch (config uses ...)` 插件配置错误不再甩英文原文——提示这是 SoWork 自身安装问题、给出 openclaw doctor 修复路径,并建议先切 S2 应急。
- **引擎下拉只剩 4 项**:内置演示流程 / S2 模型·我们的 Key(推荐)/ SoWork(商汤)/ 自定义命令;解释性后缀全部去掉。老配置在用 Claude/Codex 的动态补显示,不偷改任何人的引擎。
- 状态行只留关键信息:技能包必显,CLI 检测只在选中对应引擎时出现。

## [0.14.0] - 2026-08-05

### 修复(真机事故)

- **「CLI 路径」跨引擎误用根治**:该字段是共享的,切换引擎后旧路径会指向另一个工具(真机上把 codex 二进制当 opencode 跑,报 `unexpected argument '--auto'`)。现在显式路径只对文件名同族的工具生效,对不上就当没填、走自动查找;切换引擎时界面同步清空路径框并按新引擎重检。

### 简化(按"越简单越好")

- 生成引擎下拉撤下 OpenCode 选项:agentic 只保留我们自己的引擎(S2,内置外壳开箱即用)与既有本机订阅项;OpenCode 直连能力保留在引擎内部(`/v1/relay/chat/completions` 直通端点与 kind 均在),需要时可随时恢复入口。
- 「手动接入其他网关」整块折叠为高级项——模型接入面板平时只剩:一串 Key + 一键接入。
- 一键安装支持按外壳选择下载(引擎侧,含 opencode 平台包参数,UI 暂只暴露默认外壳)。

## [0.13.2] - 2026-08-05

### 交互简化(按真实使用反馈)

- **生成引擎整块折叠**:快速接入配好后这里本来就不用动——现在平时只显示一行摘要(当前引擎 + Key/就绪状态),点开才见切换与高级设置。
- **模型列表换真下拉**:此前用 datalist,会按输入框里已有的文字过滤(填着 senseaudio-s2 时 35 款只剩 3 款可见)。现在点击输入框即显示网关全部模型,输入才过滤,过滤到 0 自动回退全量;模型/视觉模型两个框都生效。
- 快速接入卡挪到面板最上方,手动接入区标注「高级」。

## [0.13.1] - 2026-08-05

### 新增

- **OpenCode 引擎(S2 备选执行外壳,开源)**:opencode 原生支持 OpenAI 兼容(chat/completions)协议,与我们的 S2 网关**零协议翻译直连**;自带 bash/edit/read/skill/task 等工具,`--auto` 非交互执行。实测:直连 chat-only 网关、工具调用真实落盘、工具结果回传闭环(11 项端到端)。
- 引擎新增 `/v1/relay/chat/completions` 纯直通端点:零翻译只代注 Key——真 Key 仍不出引擎进程(opencode 配置里只有 `{env:}` 引用与本机口令),口令错误 401。
- 界面「生成引擎」新增 OpenCode 选项,与 S2 共用同一串 Key/网关/模型配置;`--dir` 钉死任务目录。

### 说明

- 现在 S2 有两个外壳可选:**Codex(默认,安装包已内置)**走 Responses 翻译层;**OpenCode(备选)**直连更短。哪个产出质量更好,用真实标书 A/B 一单定,胜者转正为默认。
- 安装:`npm i -g opencode-ai`(不用登录);后续版本再评估把 opencode 二进制打进安装包。

## [0.13.0] - 2026-08-05

### 产出质量(核心)

- **成品质检门禁 quality_gate.py(确定性脚本,零 token)**:图片以《图片索引》落位锚点为准强制校正——放错章节自动搬正、有锚点漏打自动补插、编造图片ID剔除;重复灌注段折叠、逐字碎片合并;按章字数核验(<2000 红/<3500 黄);应答覆盖率(偏离表 vs 评分点)核验;产《成品质检报告.md》。
- **引擎完成后自动兜底**:不管模型有没有自觉跑质检,引擎都会审计交付稿,修复动作真实发生时自动用修复稿重建 Word(build_tender_docx,格式与图片同链路),完成消息展示质检结论。
- 技能包升 v5.6:第 5.5 步改为质检脚本(替代模型"配图复核"角色,省一整轮 agent);出 Word 前置质检;弱模型提示词加硬纪律(每章≥3500 字自查、图片ID只准来自索引)。
- 重复检测阈值 20→12 字符:短样板反复灌注也拦得住。

### 速度(不牺牲质量)

- 生成编排改并行:三个前置分析员一次性并行召唤、N 章撰写并行派出(逐章串行是首要时间浪费);汇总强制走 assemble 脚本拼装(禁模型整文重写);配图复核由脚本替代。综合预期:整单时长约砍半。
- 模型列表接入线程池 + 120s TTL 缓存:打开设置不再卡整个引擎;网关重复探测即时返回。
- 一键启动脚本:清华 pip 镜像优先(20s 超时防死源),官方源兜底。

### 网页端稳定性

- **SSE 事件流改 async 生成器**:此前每个网页连接占死一条线程池线程数小时,多标签/多次重连会把线程池耗干,表现为"启动很久然后断开、agent 全部卡住"——根治。
- SSE 响应加 `X-Accel-Buffering: no` 与 no-cache:nginx 反代默认缓冲导致页面长时间无事件的问题修复;空闲心跳 10s 一次。
- 页面重新可见时自动补一次任务同步并重挂事件流(移动端/后台标签被浏览器挂起后不再假死)。
- uvicorn 关闭访问日志(SSE 轮询刷屏)、keep-alive 65s。

### 交互

- **新建任务向导**:拖入(支持多文件与整个文件夹,保留目录结构)→ 确认招标文件(自动识别,可改)→ 选填要求 → 「开始生成」;支持「先存着,稍后开始」,待开始任务在标题旁一键启动。招标文件永远落任务根目录、绝不混进素材(素材污染是内容变薄根源之一);无素材时不再创建空 素材/ 目录顶掉全局素材库。
- **快速接入卡**:一串 S2 Key 三件事全配好(生成引擎+对话+识图),三步实时打勾,配完即测。
- 上传路径穿越防护;旧的单文件调用完全兼容。

### 演示素材包

- 新增《演示素材包》:真实脱敏招标书(代码审核平台)+ 全套投标方素材 + 6 张语义化命名的产品截图 + **带落位锚点的图片索引** + 产品能力表(配图列)+《三步看到效果》说明。

## [0.12.1] - 2026-08-05

### 新增

- **执行外壳(Codex 二进制)内置进安装包**（CI 构建时按平台打入 dmg/exe）：S2 引擎用户不装 Node、不跑命令，填 Key 即用；引擎优先使用内置版本（与协议中转实测配套的 0.146.0）。
- **「一键安装执行外壳」按钮**（本地运行包/未内置场景）：引擎自行下载（约 130MB，npmmirror 镜像优先、官源兜底），带进度百分比；重复点击幂等，文件损坏自动重下修复；不需要管理员权限，不碰系统目录。
- **预置配置 `preset_config.json`**：定制发包时放在引擎旁（或 `BID_PRESET` 指定），首次启动自动作为配置种子，可连 Key 预填好；仅在无配置时生效，绝不覆盖老用户设置；公开发布包不携带。

### 改进

- 「测试连接」缺外壳时直接指向一键安装按钮，不再要求用户开终端。

## [0.12.0] - 2026-08-05

### 新增

- **「S2 模型 · 用我们发的 Key」生成引擎**：别人拿到中标狗后不再需要 Claude/Codex 账号或任何登录，只填一串我们发的 Key 就能产真实标书，额度走我们的 token 套餐。本机装一次执行外壳 `npm i -g @openai/codex` 即可（不登录、不消耗其订阅额度）。
- 引擎内置 Responses↔chat 协议中转（`/v1/relay/*`）：Codex CLI 只认 Responses API，而 S2 网关只有 chat/completions，两边由随应用分发的本地引擎翻译，不多装进程、不改用户原有 `~/.codex`。
- S2 引擎「测试连接」分层报错：Key 未填 / 网关 401·429 / 模型不在套餐（列出可用模型）/ 未装 Codex CLI / 中转层原因，全部为中文下一步动作。
- 新文档《让别人用S2模型.md》：客户三步接入话术、Key 发放与安全边界、故障对照表。

### 安全

- 真实 Key 不进 Codex 环境变量：Codex 拿到的是本机随机中转口令，真 Key 只存在引擎进程内；页面永不回显 Key。
- S2 引擎使用独立 CODEX_HOME（数据目录内自动生成），用户原有 Codex 登录态与订阅配置零改动。

## [0.11.2] - 2026-08-04

### 新增

- Release 同时提供包含完整源代码和一键启动脚本的本地运行版 ZIP。

### 修复

- 修复旧版标书助手引擎长期占用 8848 时，新版误连旧接口导致“打开文件”和“打开任务文件夹”失败的问题。
- 新版桌面引擎改用 8849，并在连接前校验文件打开能力；缺少能力的旧引擎不会再被当作可用引擎。
- 修正源码包中的 Mac、Windows 一键启动脚本工作目录。

## [0.11.1] - 2026-08-04

### 新增

- 任务完成消息提供“打开最终 Word”和“打开任务文件夹”直达入口。
- 每个产出文件显示类型、用途和建议查看顺序。

### 改进

- 桌面端产出操作由“下载”改为调用系统默认应用直接打开；网页模式仍保留下载。
- 产出按“最终交付、检查报告、分析依据、过程稿件”分组，完成消息不再堆叠全部文件名。
- 正式构建时为内置 Skill 注入统一隐藏版权指纹，不影响 Skill 执行与显示。

### 修复

- 修正桌面引擎日志仍写入旧品牌数据目录的问题。

## [0.11.0] - 2026-08-04

### 新增

- 产品正式更名为“中标狗”，启用新 App 图标与网页图标。
- 素材库位置支持切换到其他目录或共享盘，并可恢复默认位置。
- 错误消息增加直接自助处理入口。
- 任务列表按进行中和已完成状态分组。

### 改进

- 首次启动自动把旧版 `文稿/标书助手` 数据迁移到 `文稿/中标狗`。
- Claude Code 与 Codex CLI 自动查找 Homebrew、npm 等常见安装位置。

### 修复

- 修复桌面端删除任务、重跑任务和清空素材操作失效的问题。

## [0.10.1] - 2026-08-03

### 新增

- 对话和文件预览支持 Markdown 标题、列表、表格、引用与行内代码。
- 对话失败后支持一键重发。

### 改进

- 消息发送后立即显示，并展示“正在回复”状态。
- 宽表格自动使用横向页面、固定列宽并执行格式门禁。
- Claude Code / Codex CLI 未安装时提供中文操作指引。

### 修复

- 修复已回答选择题重复出现的问题。
- 修复任务停止状态不明确的问题。

## 更早版本

更早版本用于桌面外壳、本地引擎、模型接入、素材库和 Agent 工作流的内部迭代，尚未维护完整公开日志。

[0.11.2]: https://github.com/shandianT/bid-dog/releases/tag/desktop-v0.11.2
[0.11.1]: https://github.com/shandianT/bid-dog/releases/tag/desktop-v0.11.1
[0.11.0]: https://github.com/shandianT/bid-dog/releases/tag/desktop-v0.11.0
