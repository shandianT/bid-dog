{
  "name": "bid-dog-offline-browser-tests",
  "private": true,
  "version": "0.19.0",
  "scripts": {
    "test": "playwright test --config=playwright.config.js"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1"
  }
}
