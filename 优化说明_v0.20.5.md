# 中标狗 v0.20.5 优化说明(体验升级 P0 第一批)

面向一线售前的体验重构第一批:界面重心从「流程」转向「交付物」,并把引擎里已有的数据
(评分点矩阵、章节节点、解析产物)变成看得见、点得动的能力。设计依据见随包提案与交互演示。

## 一、标书大纲成为任务主视图

- 任务详情中栏新增页签:**标书大纲 / 执行过程 / 对话与要求**。分段生成任务默认打开大纲。
- 大纲每章一行:状态点(排队/在写/完成/未完成)、实时字数(按产物文件统计)、点击即预览。
- 原「生成流程台」与对话完整保留在二级页签,功能不变。
- 智能体模式(旧任务)退化为「按产物逐章预览」,并明确提示单章重写需要分段生成模式。

## 二、单章重写(售前最高频诉求)

- 已完成/失败的章节,大纲行上直接「重写本章」,可附补充要求;其余章节锁定不动。
- 旧稿自动存入任务目录 **历史版本/**(如 `章节_03_施工组织设计.v1.md`),可随时找回;
  流水线内部 `.pipeline_attempts/` 仍是权威归档。
- 实现机制:补充要求与重写序号写入节点执行契约 → 本章摘要变化 → 引擎归档旧稿并重跑本章;
  汇总/质检/Word 导出本就按摘要驱动,自动联动重做,**不需要整单重跑**。
- 新端点:`POST /v1/jobs/{id}/chapters/{node_id}/rewrite`,body `{note}`。
  正在生成时返回明确拒绝文案;升级兼容:0.20.4 旧任务摘要不变,不会因升级重跑已完成章节。

## 三、评分点覆盖仪表(给售前一个能汇报的数字)

- 顶栏常驻「**评分点覆盖 M/N**」。口径经得起追问:
  规划无缺口 + 落到具体章节 + 该章节已写完,三者同时满足才算覆盖。
- 点开列出未覆盖项(要求/分值/缺口/落位);已定位到章节的可一键「**补写应答**」——
  复用单章重写,把该评分点的补写要求精确派发到对应章节。
- 新端点:`GET /v1/jobs/{id}/coverage`,数据源为流水线本就生成的《评分点响应矩阵.md》。

## 四、解析确认门(错误消灭在源头)

- 新建任务默认勾选「开始撰写前,先确认解析出的关键信息」(向导第三步可关)。
- 响应规划完成后暂停,结构化卡片展示**本地确定性提取**的:项目名、递交截止、资质要求、
  评分办法(评分点数)、废标/否决风险条数——找不到的项如实标注「未识别,请人工核对」,不猜。
- 「确认无误」即继续分章撰写;「有误,需要修正」把修正文字写入《你的要求.md》,
  响应规划的输入摘要随之变化,会按修正**重新核对规划**后再开写——真闭环,不是记个备注。
- 等待确认期间任务呈现为「需要你确认」,重启应用后确认卡仍在;对旧任务与关闭该选项的任务零影响。

## 五、报错横幅三段式

- 全部错误/完成横幅统一为:发生了什么(人话)→ 内容是否保留 → **一个主按钮** + 一个次按钮,
  其余动作收进「更多 ▾」。主按钮永远是最推荐的下一步(后端 actions 的第一项)。

## 六、Word 样式预览

- 预览抽屉对 md 产物新增「Word 视图 / 原文」切换,默认 Word 视图:宋体正文、首行缩进、
  居中黑体标题、实线表格——导出同款版式示意,售前一眼能判断「这能不能交」;
  底部注明最终以导出 Word 为准。

## 验证

- Python 回归:`tests/test_p0_features.py` 新增 11 项(覆盖度口径、重写归档/契约/并发拒绝/
  失败重置、确认门、修正闭环、建单标志),全量套件通过。
- 浏览器交互:`tests/check_p0_ui.spec.js` 新增 4 项(大纲页签与章节状态、覆盖仪表与定点补写、
  重写弹窗请求体、确认卡确认/修正),连同既有 13 项全部通过;前端逻辑检查 47 项通过。
- 受限环境跑浏览器测试可设 `BIDDOG_CHROMIUM=/path/to/chromium`;不设置时与之前行为一致。

## 建议的下一批(P1,见随包提案)

可交互出件前清单、废标红线实时标红、素材库卡片化、版本 diff、进度可信化、深色模式等。
